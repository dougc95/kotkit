<!-- Generated from the decomposition run of 2026-09-07. Each unit is one deliverable a coding agent can complete and verify alone with only its "after:" units done. Backend/shared units carry impl + unit + integration tests; frontend units carry component + state + api + validation. The exhaustive brief for every id (files, named test cases, spec references) is in tasks-detail.md under the same id. Design decisions D16–D40 in design.md are binding where a unit's text and an older table disagree. -->

## 1. Kickoff and workspace

- [x] 1.1.1 Pin registry-verified dependency versions and commit the lockfile — impl: create apps/api/package.json (`@attention-lab/api`) and apps/web/package.json (`@attention-lab/web`) with empty scripts, then `npm install <pkg>@<major.minor> --save-exact -w <workspace>` per design.md's 6-Sep registry (react 19.2, vite 8.2, fastify 5.12, drizzle-orm 0.45, typescript 5.9.3 never 7.x, vitest 5.0.x, @playwright/test 1.63, etc.); an install-time ERESOLVE is handled on the spot per D3 by stepping only the offending package back one major, recorded for 1.1.2; verify: `npm install && npm ls --depth=0 --workspaces --include-workspace-root` exits 0 with no invalid/missing/extraneous/ERESOLVE lines, `package-lock.json` is committed and resolves react, vite, fastify, drizzle-orm, `@playwright/test`, tsx.
- [x] 1.2.2 Create apps/web Vite React workspace with the /api dev proxy — component: App placeholder `<main>Attention Lab</main>`, main.tsx entry mounting to #root; state: none; api: no calls yet, Vite server.proxy forwards /api to http://127.0.0.1:8787 so later fetches use relative /api/v1 URLs; validation: no inputs — build/dev-server checks only, since the jsdom/Testing Library component-test harness is 7.1.1's alone (D16), not this unit's; verify: `npm run build -w @attention-lab/web` emits dist/index.html with id="root", then `npm run dev:web` serves 200 at 127.0.0.1:5173; after: 1.1.1.
- [x] 1.3.1 Update .env.example and create the LIMITATIONS.md skeleton — impl: document DATABASE_URL_TEST, the shared <repo root>/.env load point, and IDENTITY_MODE's accepted values (local-demo binds to 127.0.0.1/localhost/::1 only per D35, real refuses listing its four missing prerequisites); drop the DEFAULT_HIDE_TIMER/DEFAULT_END_CHIME/DEFAULT_VISIBILITY_CONTEXT block since preference defaults, including milestoneAnnouncements=false, are constants in code (D22); add an optional WEB_DIST_DIR setting noting 3.2.5 serves it with an SPA fallback; create LIMITATIONS.md's ten headings, fill D2 and D7 now, leave the rest for 10.4.1; verify: a node script asserts every required literal and heading (including WEB_DIST_DIR and ::1) and prints ENV_DOCS_OK.
- [x] 1.5.1 Configure Vitest in packages/shared and apps/api with env setup — impl: packages/shared/vitest.config.ts and apps/api/vitest.config.ts (include test/**/*.test.ts, passWithNoTests, api additionally fileParallelism:false and setupFiles:['./test/setup/env.ts'] loading <repo root>/.env); adds apps/api's test script; apps/web's Vitest+jsdom+Testing-Library harness is 7.1.1's alone (D16) — not touched here; verify: npm test exits 0 with both @attention-lab/shared and @attention-lab/api sections reporting 'No test files found' via passWithNoTests; after: 1.1.1.
- [x] 1.2.1 Create apps/api Fastify skeleton with dev, start and typecheck scripts — impl: `@attention-lab/api` (from 1.1.1) runs from TypeScript source under tsx in every mode per D37 (no compiled artifact; build = typecheck = `tsc --noEmit`); src/server.ts exports `buildServer()` (Fastify + pino, empty `/api/v1` plugin) and `start()` reading API_HOST/API_PORT with no defaults, guarded so it only fires when server.ts is the entry module; engines.node `>=22.9`; static serving of the built web app under `WEB_DIST_DIR` (D37) is added later by 3.2.5, not here; unit: 3 cases — 404 with no routes, never listens, importing starts no listener; integration: n/a, first inject-against-Postgres tests land in 3.2; verify: `npm run dev:api` prints 'listening', `/api/v1/me` returns 404, `npm run test:api` passes; after: 1.1.1, 1.5.1.
- [x] 1.2.3 Add packages/shared index barrel and import it from both apps — impl: src/index.ts re-exports ./domain/types; add @attention-lab/shared as a workspace dependency of both apps; prove resolution with live imports — apps/api/src/server.ts re-exports type Realm, apps/web/src/App.tsx renders a line computed from FIRST_SWITCH_CAP_SECONDS; unit: 6 cases in packages/shared/test/index.test.ts, 'barrel exports assertSameRealm/isReported/FIRST_SWITCH_CAP_SECONDS', 'demo+pilot throws RealmMixingError with "Simulated and real results are never combined"', 'isReported(null) false, isReported(0) true'; integration: n/a, realm mixing over stored rows covered by 6.2; verify: typecheck/build exit 0, test:domain passes 6 cases; after: 1.2.1, 1.2.2, 1.5.1.
- [x] 1.3.2 Refuse API boot when required settings are missing, naming them — impl: validateRequiredSettings(env) checks DATABASE_URL, API_HOST, API_PORT, IDENTITY_MODE in order (missing = absent, empty, or for API_PORT non-integer; WEB_ORIGIN and DEFAULT_* stay optional), formatRefusal(missing) composes the refusal line naming .env.example and both accepted IDENTITY_MODE values, start() runs the validator before listen and process.exit(1) on failure with no default ever substituted; enumeration/host/real-prerequisite checks stay 3.1's; unit: 6 cases including 'empty env → missing in declared order', 'API_PORT="abc" → missing includes API_PORT'; integration: n/a (process exits pre-route) — boot.process.test.ts spawns the real process instead, 4 cases; verify: with .env moved aside, start prints the refusal, exits 1, nothing listens; after: 1.2.1, 1.3.1.
- [x] 1.4.1 Add drizzle config, empty schema barrel and db:push script — impl: drizzle.config.ts reads DATABASE_URL from <repo root>/.env via process.loadEnvFile (throws naming DATABASE_URL if unset), schema src/db/schema/index.ts (empty barrel until 3.3 adds one file per aggregate), scripts db:push/db:generate/db:migrate; migrations are committed, never gitignored; verify: fresh attention-lab-pg container, npm run db:push exits 0 — drizzle-kit 0.31 treats the empty barrel as a no-op, re-satisfied once 3.3's first table lands if a later drizzle-kit version errors on zero tables instead; after: 1.2.1, 1.3.1.
- [x] 1.4.2 Add test-database bootstrap creating attention_lab_test and migrating it — impl: scripts/bootstrap-test-db.ts exports parseDatabaseName, maintenanceUrl, bootstrapTestDb(env) — refuses when DATABASE_URL_TEST is absent, shares its database name with DATABASE_URL, or lacks the _test suffix; CREATE DATABASE if missing; runs drizzle's migrate() when meta/_journal.json exists; logs 'test database <name> ready'; root/api db:test scripts delegate like db:push; 1.5.2 wires this as Vitest globalSetup; unit: 5 cases on parsing/refusals; integration: 3 cases against a throwaway attention_lab_bootstrap_test database dropped in afterAll; verify: two consecutive npm run db:test runs exit 0, second logs 'already exists'; after: 1.4.1.
- [x] 1.5.2 Wire the test-database bootstrap as the apps/api Vitest globalSetup — impl: apps/api/test/setup/globalSetup.ts loads <repo root>/.env and awaits bootstrapTestDb(process.env) once before any test file, registered as vitest.config.ts's globalSetup; no teardown drops the database (suites truncate). This is the single test-DB globalSetup (D16) — 3.2.1 and every later API unit reuse it rather than adding a second one; verify: npm run test:api logs 'test database attention_lab_test ready' before the first test file and the database exists in pg_database; after: 1.4.2, 1.5.1.
- [x] 1.5.3 Configure Playwright with axe in e2e/ and a toolchain smoke spec — impl: e2e/playwright.config.ts defines a toolchain chromium project (no webServer needed) plus toolchain.smoke.spec.ts asserting AxeBuilder is a constructor; per D16, 9.1.1 adds the acceptance project with the real webServer (npm run build then WEB_DIST_DIR=apps/web/dist API start, D37) and 7.1.5 adds e2e/support/{demo,clock,copy,a11y}.ts, reused everywhere after; root e2e, e2e:install, typecheck:e2e scripts added, root typecheck chained to include e2e; verify: npm run e2e:install && npx playwright test -c e2e/playwright.config.ts --list lists 1 test, npm run typecheck:e2e exits 0; after: 1.1.1, 1.2.2.
- [x] 1.1.2 Run the D3 compatibility gate and record final pins in LIMITATIONS.md — impl: add type-only pins.smoke.ts files in apps/web and apps/api importing a public type from each pinned package, run typecheck/build/db:push/playwright --list/boot checks, step any conflicting package back one major per D3 and repeat until green, fill LIMITATIONS.md's D3 table and add scripts/check-pins.mjs comparing each pinned cell to package-lock.json; verify: npm run typecheck && npm run build && npm run db:push && npx playwright test -c e2e/playwright.config.ts --list && node scripts/check-pins.mjs all exit 0, last prints PINS OK, and 1.2.1/1.2.2 verify unchanged; after: 1.2.1, 1.2.2, 1.2.3, 1.3.1, 1.4.1, 1.5.1, 1.5.3.
## 2. Shared domain rules and contracts

- [x] 2.1.1 Calendar date arithmetic for program Days 0–14 — impl: `packages/shared/src/domain/calendar.ts` exporting `LocalDate` (YYYY-MM-DD), `PROGRAM_LENGTH_DAYS = 14`, `isValidLocalDate`, `addDays` (Date.UTC-at-noon field arithmetic, no timezone), `localDateForProgramDay`, `programDayForLocalDate` (raw offset, negative before Day 0, >14 after, never clamped), `finalDate`, `isWithinProgram`, `programDates` (15 dates); invalid input throws; exported from index.ts; unit: 7 cases incl. 'finalDate is baseline + 14 calendar days', 'date before baseline returns a negative day and Day 15 returns 15 (not clamped)', 'leap day 2028-02-28 + 1 = 2028-02-29'; integration: n/a (pure; covered by 4.2.3 and 4.5.3); verify: `npm run test -w @attention-lab/shared -- domain/calendar.test` → `Tests  7 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 1.2.3, 1.5.1
- [x] 2.1.2 Current program day from an instant in the stored timezone — impl: extend `calendar.ts` with `localDateAt(instant, timeZone)` (Intl.DateTimeFormat en-CA formatToParts), `isValidIanaTimezone` (RangeError → false, '' → false; reused by 3.5.1 and 4.1.2), `currentProgramDay(program {baselineDate, timezone}, now) → {day, localDate}` consulting ONLY the program timezone with `now` supplied by the caller so the D8 demo clock (`ctx.now`) works unchanged; day never clamped; unit: 6 cases incl. 'fall-back DST yields exactly 15 distinct non-decreasing dates', 'profile timezone change on Day 8 does not move program days', 'demo offset of 14 days resolves to Day 14 = finalDate'; integration: n/a (pure; covered by 4.5.3 and 3.5.2); verify: `npm run test -w @attention-lab/shared -- calendar.timezone` → `Tests  6 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.1.1
- [x] 2.3.1 Benchmark eligibility with explicit exclusion reasons — impl: `packages/shared/src/domain/eligibility.ts` exporting `EligibilityInput` (completeInterval boolean|null, recallLockedAt, recallScores, episodeCount, materiallyDisrupted, timerQuality, sessionLocalDate, slotAssignedLocalDate, excludedByAmendment, realm, timeSource) and `evaluateEligibility → {eligible, exclusionReasons[]}` emitting reasons in ExclusionReason declaration order: interval_incomplete (null = expired but never confirmed, D24), recall_missing, scoring_incomplete, count_unknown (explicit 0 passes), materially_disrupted, timer_uncertain, timing_deviation (D23), excluded_by_amendment, simulated_time (pilot + non-measured); E never disqualifies; unit: 20 cases incl. 'completeInterval null → interval_incomplete — expiry never proves completion', 'incomplete attempt finalized without recall → [interval_incomplete, recall_missing, scoring_incomplete]' (D25), 'explicit 0 for S is not count_unknown', 'demo realm never yields simulated_time'; integration: n/a (pure; covered by 5.8.4, 5.5.1, 5.9.3); verify: `npm run test -w @attention-lab/shared -- domain/eligibility.test` → `Tests  20 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.1.1
- [x] 2.7.1 Extend types.ts with the literal unions and const arrays shared by domain and contracts — impl: append-only `as const` arrays with matching unions (`SESSION_EVENT_TYPES`, `SESSION_KINDS`, `SESSION_LIFECYCLES`, `PROGRAM_STATUSES`, `RECALL_FLAGS`, `FEED_DEVICES`, `MEASUREMENT_SCOPES`, `FEED_SOURCES`, `CHECKIN_STATUSES` complete|incomplete|not_reported, `CHECKIN_FIELDS` sleep|feed, `RESULT_STATES` in D7.4 order, `IDENTITY_MODES`, `TRANSITION_TYPES`, `CLOCK_GAP_RESOLUTIONS`, `FIRST_SWITCH_KINDS/METHODS`, `BLOCK_STATUSES`, `NEXT_ACTION_KINDS`, `DEMO_SCENARIO_NAMES`, `FEED_PLATFORM_ALL = 'all'`) plus arrays for existing unions incl. `EXCLUSION_REASONS`; widen `AttemptSummary.firstSwitch` and `countMethod` to nullable (null only when S was not reported; never eligible) and `assertSameRealm` to any `{realm}` rows; no countOrZero helper; unit: 7 cases incl. 'EXCLUSION_REASONS equals the key set of EXCLUSION_REASON_COPY', 'no export matches /orZero|zeroIf|defaultZero|toZero/i', 'assertSameRealm throws RealmMixingError for demo + pilot'; integration: n/a (types; exercised by 5.8.4 and 6.2.1); verify: `npm run test -w @attention-lab/shared -- domain/types` → `Tests  7 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 1.2.3, 1.5.1
- [x] 2.2.1 Event tallies with void, visibility and agent-check rules — impl: `packages/shared/src/domain/firstSwitch.ts` exporting `EventLike`, `countedEvents` (drops voided rows and pause/resume/clock_gap/visibility, D9/D15), `episodeEvents` (off_task plus agent_check with `details.alsoOffTask === true`, sorted by elapsedMs, D11), `tallyEvents → {offTask, external, agentChecks}` with NO combined field, `prefillCounts` (all null with countMethod null when no counted event exists, otherwise the three tallies with countMethod 'event', D31); unit: 9 cases incl. 'visibility events contribute to no tally and no prefill (hidden tab creates no episode)', 'agent_check with alsoOffTask increments offTask and agentChecks by 1, nothing summed across tallies', 'voided duplicate is excluded', 'no events → prefill all null, never 0'; integration: n/a (pure; covered by 5.2.2/5.2.3 and 5.3.2); verify: `npm run test -w @attention-lab/shared -- domain/tally` → `Tests  9 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.1
- [x] 2.2.2 Derive and format the three-state FirstSwitch — impl: extend `firstSwitch.ts` with `deriveFirstSwitch({events, episodeCount, countMethod, estimateSeconds}) → {firstSwitch, method}|null`: S null → null; S 0 → none_capped; S>0 with countMethod 'event' and a timed earliest episode → known (floor ms/1000, method event); else estimate 1..1199 → known (method estimate); else unknown; retrospective S is never summed with event tallies; `formatFirstSwitch` → '20+, capped' | 'm:ss' | 'Unknown'; `toStoredFirstSwitch`/`fromStoredFirstSwitch` map the three `first_switch_*` columns (all null for null); unit: 11 cases incl. 'S=0 → none_capped formats "20+, capped"', 'S=3 retrospective without estimate → Unknown, never 20+', 'S null → null (never none_capped, never unknown)', 'retrospective S=4 with two off_task events stays 4, never 6'; integration: n/a (pure; covered by 5.8.4); verify: `npm run test -w @attention-lab/shared -- domain/firstSwitch` → `Tests  11 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.2.1
- [x] 2.3.2 Recall flag thresholds and blank-aware recall scoring — impl: `packages/shared/src/domain/recall.ts` (D16) exporting `RECALL_DELAY_FLAG_SECONDS = 600`, `RECALL_OVERRUN_FLAG_SECONDS = 210`, `RECALL_POINT_COUNT = 5`, `RECALL_WINDOW_SECONDS = 180`, `recallDelaySeconds(intervalEndedAt, recallStartedAt)` (floor; throws when recall precedes the interval end), `deriveRecallFlags({delaySeconds, durationSeconds}) → RecallFlag[]` (strictly greater than each threshold; flags, never exclusions, D7.3), `isBlankPoint`, `scoreRecall(points, scores|null) → {recallScores|null, recallScore: ReportedCount, complete}` (blank points forced to 0; any non-blank point without a score → recallScore null AND recallScores null, never a partial array); unit: 15 cases incl. 'delay 600 → no flags, 601 → [recall_delayed]', 'non-blank point with null score → null/null/incomplete', 'all five blank → recallScore 0 (a measurement, not null)', 'RecallFlag is not assignable to ExclusionReason'; integration: n/a (pure; covered by 5.7.2 and 5.8.3); verify: `npm run test -w @attention-lab/shared -- domain/recall` → `Tests  15 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.1
- [x] 2.4.1 Slot candidates, sample counts and comparison math — impl: `packages/shared/src/domain/comparison.ts` exporting `SlotKey`, `selectSlotCandidates` (the single eligible attempt per slot; midpoint ignored; two eligible in one slot throws), `sampleCounts → {baselineEligible, finalEligible}`, `computeComparison(attempts) → ComparisonResult|null` calling `assertSameRealm` first, null unless all four candidates exist, throwing (never coalescing) on any null episodeCount/recallScore/firstSwitch/countMethod, returning s0, s14, absoluteChange, percentageReduction (null when s0 = 0), lowBaseline (s0 < 3), recall means, per-slot firstSwitches and firstSwitchMeanSeconds only when all four are known; unit: 12 cases incl. 'comparable 6,4 → 3,3: s0 5, s14 3, percentage 40', 'zero baseline → percentageReduction null and every numeric field finite', 'demo + pilot → RealmMixingError, no partial result', 'candidate with null episodeCount throws, never 0', plus a `@ts-expect-error` proving practice rows cannot be an AttemptSummary; integration: n/a (pure; covered by 6.2.1); verify: `npm run test -w @attention-lab/shared -- comparison.math` → `Tests  12 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.2.2, 2.7.1
- [x] 2.4.2 Result-state precedence and PRD copy — impl: extend `comparison.ts` with `RESULT_STATE_COPY: Record<ResultState, {headline: string|null, message, action}>` (messages verbatim from the PRD §5 table plus the spec's 'The reported switch count did not change.'; headline 'Fewer reported switches' only for improvement_maintained_recall, D38), `CAUSE_NOTE = 'Observed change does not establish cause.'`, `resolveResultState({baselineEligible, finalEligible, finalAttemptsExist, day14Finished = currentDay > 14, comparison}) → ResultState` evaluated in the fixed D7.4 order baseline_pending → final_pending → insufficient_samples → zero_baseline → more_switches → unchanged → fewer_switches_lower_recall → improvement_maintained_recall; unit: 14 table rows incl. 'two baseline, no finals, Day 15 → insufficient_samples', '0 → 1 → zero_baseline, not more_switches', 'PRD mixed 6,4 → 3,3 with recall 4 → 2 → fewer_switches_lower_recall', 'no copy contains "attention +", "%", "significan" or "confidence"'; integration: n/a (pure; covered by 6.2.2); verify: `npm run test -w @attention-lab/shared -- resultState` → `Tests  14 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.4.1
- [x] 2.4.3 Comparability warnings between same-label attempts — impl: extend `comparison.ts` per D7.5 with `ComparabilityWarning {label, field: deviceFormat|language|materialLevel|accommodations, baseline, final, message}` and `comparabilityWarnings(candidates) → ComparabilityWarning[]` comparing baseline:A with final:A and baseline:B with final:B only when both slot candidates exist; accommodations compared as sets; null vs value is a difference whose message says 'not recorded at baseline' / 'not recorded at final'; reads `conditions` only and never touches eligibility; unit: 7 cases incl. 'final A has increased_font_size, baseline A none → one warning naming accommodations', 'same accommodations in different order → no warning', 'warnings leave every eligible/exclusionReasons value untouched (pure)'; integration: n/a (pure; covered by 6.2.3); verify: `npm run test -w @attention-lab/shared -- comparability` → `Tests  7 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.4.1
- [x] 2.5.1 Band ceilings and practice-block qualification predicates — impl: `packages/shared/src/domain/progression.ts` exporting `BandCeiling`, `DEFAULT_BAND_CEILINGS` (Days 1–3 10 min, 4–7 15, 8–10 20, 11–14 25; the value stored in revision settings `bandCeilings`), `bandCeilingSeconds(day, ceilings = DEFAULT_BAND_CEILINGS)` (Day 0 → 600; days beyond 14 use the last band), `PracticeBlockRecord`, `blockQualifies(block, currentTargetSeconds)` = finalized && practice && targetSeconds === current && completeInterval === true && outputQuality 'yes' && episodeCount !== null && ≤ 1, `dayQualifies` = at least two qualifying blocks; unit: 11 cases incl. 'episodeCount null does not qualify (unknown is not ≤ 1)', 'benchmark kind never qualifies', 'unfinalized block that reached its target never qualifies (expiry is not completion)', 'custom ceilings from a revision override the defaults'; integration: n/a (pure; covered by 4.5.3); verify: `npm run test -w @attention-lab/shared -- progression.bands` → `Tests  11 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.1.1, 2.7.1
- [x] 2.5.2 Two-consecutive-day progression suggestion — impl: extend `progression.ts` with `PROGRESSION_STEP_SECONDS = 300`, `PROGRESSION_ACCEPTED_REASON = 'progression accepted'`, `suggestProgression({day, currentTargetSeconds, blocks, bandCeilings?}) → {suggestedTargetSeconds, qualifiedOn: [LocalDate, LocalDate]} | null`: throws RealmMixingError on mixed realms, groups blocks by localDate, needs a pair of adjacent dates both qualifying at the current target (qualification is held through later missed days), suggests current + 300 only when ≤ bandCeilingSeconds(day); benchmark blocks ignored; unit: 11 cases incl. 'Days 4–5 qualify at 900 → Day 6 null, Day 8 suggests 1200', 'missed Days 6–7 then Day 8 → still suggests (nothing reset)', 'after acceptance at 1200 the 900 blocks no longer qualify', 'benchmark sessions never yield a 1200 suggestion'; integration: n/a (pure; covered by 4.5.3 and 4.4.1); verify: `npm run test -w @attention-lab/shared -- progression.suggest` → `Tests  11 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.5.1
- [x] 2.6.1 Feed row validation with the platform-all rule and scope-aware aggregates — impl: `packages/shared/src/domain/feed.ts` exporting `FeedRowInput`, `validateFeedRows → {ok} | {ok: false, errors: {index, code: feed_subset_violation|duplicate_feed_row|feed_platform_conflict, message}[]}` (codes equal the D19 error codes; D36: a device-level row is platform 'all' with scope feed and source estimate, and a device has either its 'all' row or platform rows, never both; no allowance rule — overruns are valid) and `feedAggregates → {unitLabel 'device-minutes', feedDeviceMinutes (feed-scope sum, null when none), feedByDevice (null per device without a feed-scope row; 'all' is the device total), partial (phone or desktop lacks a feed-scope row), shortVideoDeviceMinutes (subset, never added), appTotals[]}`; unit: 13 cases incl. 'no rows → null, never 0', 'short-video 45 on 30 → feed_subset_violation', 'phone all 30 + phone instagram feed 10 → feed_platform_conflict', 'app_total 60 beside feed 25 → total 25'; integration: n/a (pure; covered by 6.1.2); verify: `npm run test -w @attention-lab/shared -- feed.aggregates` → `Tests  13 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.1
- [x] 2.6.2 Single check-in completeness status and headline values — impl: extend `feed.ts` with `checkinStatus(checkin {sleepMinutes}|null, rows) → {status: complete|incomplete|not_reported, missing: ('sleep'|'feed')[]}` — the ONE completeness implementation (D16) called by 4.5.3 and 6.1.1/6.1.2: null check-in → not_reported with both missing; complete = sleepMinutes !== null AND at least one scope-'feed' row (explicit 0 counts; an app_total-only row does not complete, D36); otherwise incomplete listing what is missing; never inferred from row existence; plus `checkinValues(checkin, rows) → {sleepMinutes, phoneFeedMinutes, desktopFeedMinutes}` (device totals from feedAggregates, nulls preserved, D22); unit: 9 cases incl. 'sleep only → incomplete missing [feed]', 'sleep + explicit-zero phone all row → complete', 'sleep + app_total-only row → incomplete', 'existing check-in with sleep null and no rows is never complete by existence'; integration: n/a (pure; covered by 6.1.1 and 4.5.3); verify: `npm run test -w @attention-lab/shared -- feed.status` → `Tests  9 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.6.1
- [x] 2.7.2 Contract primitives, error envelope, me and demo route contracts — impl: `packages/shared/src/contracts/common.ts` (`Obj` always `additionalProperties: false`, `Lit(constArray)` so contract literals cannot drift from types.ts, `ReportedCountSchema` integer ≥ 0 | null, `LocalDateSchema`, `UuidSchema`, `IsoTimestampSchema`, `IdempotencyKeyHeader`, `ERROR_CODES` = the 37 D19 codes with `ERROR_CODE_STATUS` (400/404/409/422/429), `ErrorResponse {code: Lit(ERROR_CODES), message, fieldErrors?: Record<string,string>, details?: object (D18; never note text), retryable, requestId}`); `contracts/me.ts` (`MeResponse` with preferences {hideTimerDefault, endChime, visibilityContext, milestoneAnnouncements} and demoClockOffsetSeconds; `PatchPreferencesBody`); `contracts/demo.ts` (`DemoClockBody {offsetSeconds}` absolute, `ScenarioLoadParams`, no load body, `ScenarioLoadResponse {programId: string|null}`); unit: 8 Value.Check cases incl. 'ReportedCountSchema accepts 0, 4, null; rejects -1, 1.5, "3"', 'ErrorResponse with details {activeSessionId} passes; code "oops" fails', 'ERROR_CODES has 37 distinct members'; integration: n/a (schemas; exercised by 3.2.4 and 3.5.x); verify: `npm run test -w @attention-lab/shared -- contracts/common` → `Tests  8 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.1
- [x] 2.7.3 Programs route contracts — impl: `packages/shared/src/contracts/programs.ts`: `CreateProgramBody` (practiceTargetSeconds 300|600|900, feedEstimateMinutes integer ≥ 0 | null), `ProgramResponse`, `RevisionResponse`, `SlotResponse` with `frozenAt` and `attempts: [{sessionId, lifecycle, eligible: boolean|null, excludedByAmendment}]` (D22), `NextActionSchema` union (benchmark/final variants carry slotId, phase, label, assignedLocalDate, plannedLocalTime; `final` is valid on Day 14 and after, D23), `CurrentProgramResponse` allowing program/revision null, slots [], day null (D22), `PatchProgramBody` (status only completed|archived, D33), `PutSlotsBody`, `PutSlotsResponse {program, slots, missing: [{phase, label, fields}]}`, `CreateRevisionBody` (300..1500 step 300), `TodayResponse` with `checkin: {status, missing, values: {sleepMinutes, phoneFeedMinutes, desktopFeedMinutes}}` (D22); unit: 9 cases incl. 'no-program CurrentProgramResponse validates', 'PatchProgramBody status "active" rejected', 'TodayResponse rejects a third block; missing values key fails'; integration: n/a (schemas; exercised by 4.1–4.5); verify: `npm run test -w @attention-lab/shared -- contracts/programs` → `Tests  9 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.2
- [x] 2.7.4 Sessions route contracts — impl: `packages/shared/src/contracts/sessions.ts`: `CreateSessionBody` (targetSeconds integer 300..1500 multipleOf 300, D30), `EventInput` (clock_gap variant requires `details {gapSeconds ≥ 0, resolution?}`, D26; others allow `{alsoOffTask?, reason?}`), `EventsBatchBody` 1..100, `TransitionBody {expectedVersion, type, reason?}` (reason stored only for pause, D29), `ClockGapBody`, `AgentPlanBody`, `RecallBody` (5 points, blanks allowed), `FinalizeBody {expectedEventCount, lastBatch?, review}` with review gaining `reviewNote` (D31) and `recallScores` 5 × 0|1|null, `FirstSwitchSchema`, `ReviewResponse` (every ReportedCount nullable, firstSwitch nullable, reviewNote), one `SessionResponse` exactly per D20 (stored columns camelCase, serverNow, timing {elapsedSeconds, remainingSeconds, deadlineReached, isPaused}, tallies {offTask, external, agentChecks}, eventCount, events[] with voidedAt, review, agentPlan|null, amendments[]), `FinalizeResponse {session, review, eligible: boolean|null, exclusionReasons}`, `AmendmentBody`; unit: 12 cases incl. 'targetSeconds 420 rejected, 300 and 1500 accepted', 'clock_gap details without resolution valid', 'FinalizeBody rejects eligible/exclusionReasons/recallScore/points keys', 'tallies rejects an extra total key'; integration: n/a (schemas; exercised by 5.1–5.9); verify: `npm run test -w @attention-lab/shared -- contracts/sessions` → `Tests  12 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.2
- [x] 2.7.5 Days, report, export and research route contracts — impl: `packages/shared/src/contracts/days.ts` (`FeedRowSchema` with platform 1..100 where 'all' is the device-level row, D36; `PutDayBody {expectedVersion ≥ 0, sleepMinutes?, stress? 0..10|null, mindfulnessMinutes?, note? ≤ 2000, feed[]}`; `DayResponse {checkin {localDate, sleepMinutes, stress, mindfulnessMinutes, note}, feed[], status {status, missing}, aggregates (feedAggregates shape), version ≥ 0}` whose no-row form is all-null checkin, feed [], not_reported, version 0, D22; subset and platform rules stay in feed.ts); `contracts/report.ts` (`AttemptSchema` mirroring the widened AttemptSummary plus lifecycle, replacementReason, recallFlags, revisionId, mindWanderingCount, materiallyDisrupted, timerQuality; `ReportResponse {realm, samples, attempts[], comparison?, resultState, warnings[], practice[], days[], revisions[]}`; `ExportQuery`); `contracts/research.ts` (`ResearchCardSchema`, `ResearchCardsResponse` exactly 3); unit: 8 cases incl. 'platform "all" accepted, "" rejected', 'D22 empty-day DayResponse validates', 'attempt with firstSwitch null and countMethod null accepted'; integration: n/a (schemas; exercised by 6.1–6.4); verify: `npm run test -w @attention-lab/shared -- contracts/days-report` → `Tests  8 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.2
- [x] 2.7.6 Contract barrel, realm rejection proof and cross-app Static types — impl: `packages/shared/src/contracts/index.ts` re-exporting every schema and Static type plus `REQUEST_BODY_SCHEMAS` (15 request bodies with one valid example each; scenario load has no body, D22); `apps/api/src/contracts.typecheck.ts` and `apps/web/src/contracts.typecheck.ts` assign example literals to `Static<typeof CreateProgramBody>` / `FinalizeBody` and carry a `@ts-expect-error` for a `realm` key so both apps compile the contracts under exactOptionalPropertyTypes; route-level rejection holds because 3.1.2 configures AJV `coerceTypes: false, removeAdditional: false, useDefaults: false` (D22); unit: 5 cases incl. 'every example passes and example + realm fails at path /realm', 'every object schema reachable from REQUEST_BODY_SCHEMAS has additionalProperties false', 'no request body declares realm or userId at any depth'; integration: n/a (covered by 4.1.2 and 3.2.4); verify: `npm run test -w @attention-lab/shared -- contracts/realm` → `Tests  5 passed`; `npm run typecheck` at the repo root exits 0; after: 2.7.3, 2.7.4, 2.7.5, 1.2.3
- [x] 2.8.1 Relative-time demo scenario fixture shape and the four comparison scenarios — impl: `packages/shared/src/fixtures/demoScenarios.ts` with `DemoScenario {name, description, viewDay, program|null, revisions[], slots[], sessions[], events[], reviews[], amendments[], agentPlans[], checkins[], feedRows[], expected: {resultState: ResultState|null, percentageReduction?}}` encoded per D35, every root row realm demo and every session row timeSource 'demo_clock' (D34), every session with a review row (D31), revision 1 reason 'initial plan' (D33); `anchorScenario(scenario, loadInstant)` resolves days and offsets; `attemptsOf(scenario) → AttemptSummary[]`; scenarios comparable-change, mixed-result, missing-final (final B early stop, S blank → [interval_incomplete, count_unknown]), zero-baseline with their expected states; unit: 8 cases incl. 'evaluateEligibility round-trips every finalized attempt', 'missing-final: final B episodeCount null (not 0), firstSwitch null, insufficient_samples'; integration: n/a (covered by 3.5.4 and 6.2.2); verify: `npm run test -w @attention-lab/shared -- demoScenarios.comparison` → `Tests  8 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.1.2, 2.3.1, 2.4.2, 2.7.1
- [x] 2.8.2 Four journey scenarios and the complete registry — impl: extend `demoScenarios.ts` per D35 with new-user (program null, every row array empty, expected null), working-day (viewDay 4; revision 1 target 600 'initial plan', revision 2 target 900 effective Day 4 'progression accepted'; status active; both baselines eligible; Days 1–3 two finalized practice sessions each, Day 4 block 1 finalized; check-ins Days 1–3 complete; expected final_pending), recovery (viewDay 5; one RUNNING practice session with an agent plan, an all-null review row and zero events — the unsynced batch is created client-side by 9.1.8; expected final_pending), timing-deviation (viewDay 0; status baseline_ready; baseline A RUNNING started 18 min before load, timerQuality ok, one clock_gap event `{gapSeconds: 300}` without resolution; no baseline B; expected baseline_pending); narrow `DEMO_SCENARIOS` to `Record<DemoScenarioName, DemoScenario>`; unit: 9 cases incl. 'registry keys equal DEMO_SCENARIO_NAMES', 'timing-deviation pipeline → baseline_pending'; integration: n/a (covered by 3.5.4 and 4.5.3); verify: `npm run test -w @attention-lab/shared -- demoScenarios.journeys` → `Tests  9 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.8.1, 2.5.2, 2.6.2
- [x] 2.8.3 Three curated research card fixtures — impl: `packages/shared/src/fixtures/researchCards.ts` per D13 with `RESEARCH_CURATED_ON = '2026-09-06'`, `RESEARCH_NOTE`, `DISCOVERY_NOTE = 'Automated discovery not enabled'` and `RESEARCH_CARDS` (exactly three, typed `Static<typeof ResearchCardSchema>`): Castelo et al. 2025 (PNAS Nexus), Mrazek et al. 2013 (Psychological Science), Leroy & Glomb 2018 (Organization Science), each with hedged finding, limitation and relevance text from the protocol's evidence table, provenance peerReview 'peer_reviewed' and reviewed 'abstract' (D40: recorded in LIMITATIONS.md as pending Douglas's confirmation); unit: 5 cases incl. 'each card passes ResearchCardSchema', 'no finding contains "proves" or "causes"', 'the two note constants equal the research-cards spec copy verbatim'; integration: n/a (served by 6.4.1); verify: `npm run test -w @attention-lab/shared -- fixtures/researchCards` → `Tests  5 passed`; `npm run typecheck -w @attention-lab/shared` exits 0; after: 2.7.5
- [x] 2.9.1 Repository no-coalesce guard test — impl: `packages/shared/test/tools/findZeroCoalesce.ts` using the TypeScript compiler API over packages/shared and apps/api tsconfigs to report (a) `?? 0` / `|| 0` whose left operand's checked type is a nullable number (ReportedCount-shaped), (b) regex `COALESCE(<expr>, 0)` in .ts/.sql under packages/shared/src, apps/api/src and its migrations, (c) `integer('<D7.1 nullable column>')...default(0)`; findings `{file, line, snippet, rule}`; fixtures under packages/shared/test/fixtures/coalesce/ excluded from the shared tsconfig; unit: 5 cases incl. 'fixture `count ?? 0` with count: ReportedCount is reported with file and line', 'fixture `n || 0` with non-nullable n is not reported', 'current tree: zero findings across packages/shared/src and apps/api/src (skips apps/api when its tsconfig is absent)'; integration: n/a (runtime counterpart covered by 6.3.1); verify: `npm run test -w @attention-lab/shared -- noCoalesce` → `Tests  5 passed`, and a temporary `const y = x ?? 0` under packages/shared/src makes it fail naming the file and line; after: 1.5.1, 2.2.2, 2.3.2, 2.4.3, 2.5.2, 2.6.2
## 3. API foundation

- [x] 3.1.1 Implement loadConfig with identity-mode, loopback and real-mode gates — impl: `apps/api/src/config.ts` (`ConfigError`, `AppConfig`, pure `loadConfig(env)` layered on 1.3.2's `validateRequiredSettings`, `assertLocalDemoConfig`) and `apps/api/src/preferences.ts`; IDENTITY_MODE exact-match `local-demo|real`, no fallback; local-demo requires API_HOST in `LOOPBACK_HOSTS = ['127.0.0.1','localhost','::1']` (D35) else refuse 'demo mode must never be exposed on a network interface'; real always refuses, listing missing AUTH_SECRET/GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET/AUTH_INVITE_ALLOWLIST and pointing to LIMITATIONS.md; WEB_DIST_DIR read through; `DEFAULT_PREFERENCES = { hideTimerDefault:false, endChime:false, visibilityContext:false, milestoneAnnouncements:false }` (D22, D39), no preference env variables read; unit: 21 cases incl. '::1 accepted', '0.0.0.0 refused', 'real with all four still refuses', 'DEFAULT_PREFERENCES deep-equals four false keys'; integration: n/a (process exit covered by 3.1.2 spawn tests); verify: `npm run test -w @attention-lab/api -- test/config.test.ts` → 21 pass. after: 1.2.1, 1.3.1, 1.3.2
- [x] 3.1.2 Implement buildApp factory, strict AJV options and server.ts boot with spawn tests — impl: `apps/api/src/app.ts` `buildApp(config)` (calls `assertLocalDemoConfig`, refuses any non-local-demo mode, Fastify 5 + TypeBox, `genReqId` UUID, `requestIdHeader:false`, `disableRequestLogging:true`, AJV `coerceTypes:false, removeAdditional:false, useDefaults:false` so null is never coerced to 0 (D22), plugins in fixed order under `/api/v1`, static serving last per 3.2.5); `boot.ts` `startServer`/`installSignalHandlers`; `server.ts` entry runs under tsx via 1.2.1's scripts (D37, no dist); unit: 6 cases incl. 'null reaches handler as null, not 0', 'string never coerced to integer', 'SIGTERM awaits close then exit(0)'; integration: 6 spawned-process cases incl. 'real with all four secrets exits 1', 'local-demo 127.0.0.1 port 0 listens'; verify: `npm run test -w @attention-lab/api -- test/app.test.ts test/boot.test.ts` → 12 pass. after: 3.1.1
- [x] 3.2.1 Implement db plugin and the API test foundation (buildTestApp, captureLogs, truncateAll) — impl: `apps/api/src/plugins/db.ts` (postgres-js + drizzle over the schema barrel, `app.db`/`app.sql`, onClose ends client); `test/helpers/captureLogs.ts` (in-memory pino destination with `entriesWith(reqId)`), `test/helpers/testDb.ts` `resolveTestDatabaseUrl` (DATABASE_URL_TEST, name must end `_test`, reuses 1.4.2 `parseDatabaseName`), `test/helpers/buildTestApp.ts` returning `{ app, db, sql, logs, truncateAll, close }`; reuses 1.5.1's vitest config and 1.5.2's globalSetup — no second globalSetup or .env loader; 4.1.1 and 5.1.3 build on these helpers (D16); unit: 3 cases incl. 'dev database name refused'; integration: 5 cases incl. 'current_database() is attention_lab_test', 'two build/close cycles leave no open handles'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/plugins/db.test.ts` → 8 pass, no open-handle warnings. after: 1.4.2, 1.5.1, 1.5.2, 3.1.2
- [x] 3.2.2 Implement request-id, no-store and privacy-safe logger with one completion entry — impl: `apps/api/src/logging.ts` `createLogger` (req serializer emits only id/method/url/route, res only statusCode, redacts authorization/cookie, no err serializer); `plugins/requestId.ts` (onSend sets `x-request-id` = server UUID on every response incl. 404/500; client value never trusted); `plugins/requestLog.ts` (exactly one onResponse 'request completed' entry with method, url, route, statusCode, responseTime — no per-tick telemetry); `plugins/noStore.ts` (`Cache-Control: no-store` on every `/api/v1/*` response unless route `config.public` — only `GET /research/cards` 6.4.1); unit: 3 cases incl. 'req serializer drops headers/query/params/body'; integration: 8 cases incl. 'spoofed x-request-id replaced', 'unknown-route 404 carries no-store', 'one completion entry per request'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/plugins/headers.test.ts` → 11 pass. after: 3.2.1
- [x] 3.2.4 Implement error classes, log sanitizer and the error-envelope mapper plugin — impl: `apps/api/src/errors.ts` `AppError` subclasses with the D19 vocabulary (400 `malformed_request`; 404 `not_found` 'Not found' for missing and not-owned alike; 409 `program_exists, active_session_exists, idempotency_mismatch, stale_version, slot_frozen, event_count_mismatch, already_finalized, session_not_active, session_not_ended, interval_not_ended, recall_locked, not_finalized, invalid_status_transition, program_terminal`; 422 `realm_mismatch, feed_subset_violation, feed_platform_conflict, duplicate_feed_row, baseline_times_too_close, readiness_regression, before_slot_date, slot_full, replacement_reason_required, eligible_attempt_not_retaken, practice_only, benchmark_only, benchmark_only_field, practice_only_field, impossible_offset, invalid_transition, recall_before_interval_end, recall_not_locked, effective_day_in_past, date_locked`; 429 `write_limit` reserved, no limiter); envelope `{ code, message, fieldErrors?, details?, retryable, requestId }` with D18 `details` (never note text); `sanitizeErrorForLog`; `plugins/errors.ts` maps validation → fieldErrors without echoing values, `RealmMixingError` → fixed message; unit: 10 cases; integration: 12 cases incl. 'null reaches handler as null', 'submitted value absent from 400 body', 'internal error logs no SECRET'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/plugins/errors.test.ts` → 22 pass. after: 2.7.2, 3.2.2
- [x] 3.3.1 Drizzle schema: user_profiles, programs, protocol_revisions, benchmark_slots + migration 0000 — impl: `apps/api/src/db/schema/{userProfiles,programs,protocolRevisions,benchmarkSlots}.ts` + barrel; enums realm/program_status/benchmark_phase/slot_label; `programs.realm` NOT NULL with no default (server stamps it), partial unique `programs_one_open_per_user` WHERE status IN (draft, baseline_ready, active), `feed_estimate_min` nullable no default, `current_revision_id` nullable circular FK, revisions UNIQUE(program_id, revision), slots UNIQUE(program_id, phase, label) with nullable language/device_format/material_level/planned_local_time and `frozen_at`; migration `0000_profiles_programs_slots` generated, reviewed, committed; unit: 5 getTableConfig cases incl. 'realm has no default'; integration: 8 cases incl. 'second open program → 23505', 'program without realm → 23502', 'feed_estimate_min omitted reads null, never 0'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/db/programs.schema.test.ts` → 13 pass; `drizzle-kit check` exits 0. after: 1.4.1, 3.2.1
- [x] 3.2.3 Implement identity context plugin with demo clock, realm stamp and owner guards — impl: `apps/api/src/plugins/identity.ts`: `LOCAL_DEMO_PRINCIPAL_ID = 'local-demo'`, pure `deriveContext({ identityMode, offsetSeconds, realNow })` → `{ principalId, realm:'demo', now = realNow + offset, timeSource = offset === 0 ? 'measured' : 'demo_clock' }` (D8), ConfigError for any other mode; `ensurePrincipalProfile(db)` inserts the profile (timezone 'UTC', DEFAULT_PREFERENCES, offset 0) on ready and after `truncateAll`; `onRequest` reads the offset per request into `request.ctx`, ignores `x-principal-id`/`x-realm`; `ownerStamp(ctx)`, `assertOwnRealm` (422 realm_mismatch), `assertOwnedBy` (404, never 403); unit: 7 cases incl. 'offset 86400 → demo_clock', 'other user_id → 404 Not found'; integration: 5 cases incl. 'spoofed headers ignored', 'offset 1209600 shifts ctx.now', 'profile re-seeded after truncateAll'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/plugins/identity.test.ts` → 12 pass. after: 3.2.1, 3.2.4, 3.3.1
- [x] 3.3.2 Drizzle schema: sessions aggregate with nullable counts + migration 0001 — impl: `apps/api/src/db/schema/{focusSessions,sessionEvents,sessionReviews,sessionAmendments,agentPlans}.ts`; `focus_sessions.realm` NOT NULL no default (realm stays on the three roots only, D34 — events, reviews, amendments, plans inherit through the session), CHECK `focus_sessions_benchmark_has_slot` ((kind='benchmark') = (slot_id IS NOT NULL)), partial unique `focus_sessions_one_active_per_user` WHERE lifecycle IN (running, paused, awaiting_review), `clock_gap_seconds` NULL; `session_events` UNIQUE(session_id, client_event_id), `voided_at`; `session_reviews` five count columns integer NULL with NO default, `first_switch_kind` enum exactly none_capped|known|unknown, `review_note text NULL` (D31) beside `output_note`, `observed_conditions` jsonb NOT NULL; migration `0001_sessions`; unit: 7 cases incl. 'every count column nullable without default', 'review_note nullable'; integration: 10 cases incl. 'benchmark != practice enforced by CHECK', 'review with counts omitted reads null; explicit 0 reads 0', 'ON CONFLICT DO NOTHING reports 0 rows'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/db/sessions.schema.test.ts` → 17 pass; `drizzle-kit check` exits 0. after: 3.3.1
- [x] 3.3.3 Drizzle schema: daily_checkins, feed_usage, mutation_receipts + migration 0002 — impl: `apps/api/src/db/schema/{dailyCheckins,feedUsage,mutationReceipts}.ts`; `daily_checkins.realm` NOT NULL no default (third realm root, D34), `sleep_minutes` NULL, CHECK `daily_checkins_stress_range`, UNIQUE(program_id, local_date), no status column (completeness is computed on read by shared `checkinStatus`, D16/D22); `feed_usage` UNIQUE(checkin_id, device, platform, measurement_scope) — headline rows use platform 'all' (D36; the all-vs-platform conflict is a 6.1.2 service rule, not a constraint), CHECK `feed_usage_short_video_subset`, no ON DELETE; `mutation_receipts` PK(user_id, idempotency_key) with request_hash, result_ref, expires_at; migration `0002_days_receipts`; unit: 4 getTableConfig cases; integration: 10 cases incl. 'short_video 40 > minutes 30 → 23514', 'explicit zero feed row reads 0 while omitted sleep reads null', 'same key under another user allowed'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/db/days.schema.test.ts` → 14 pass; `drizzle-kit check` exits 0. after: 3.3.1
- [x] 3.3.4 Review committed migrations and add from-scratch apply and integrity tests — impl: review `src/db/migrations/0000_*.sql`, `0001_*.sql`, `0002_*.sql` and `meta/_journal.json` (additive only, every named index/constraint present, no `DEFAULT 0` on any nullable count, `review_note` present without default, realm_enum on exactly programs/focus_sessions/daily_checkins, no ON DELETE); confirm 1.5.2's globalSetup applies them via `migrate()`; add `apps/api/test/db/migrations.test.ts` and a `migrations:check` script (`drizzle-kit check`); the from-scratch test drops both `public` and drizzle's `drizzle` schema so migrate() re-applies; unit: 4 static SQL cases; integration: 6 cases incl. 'from-scratch migrate applies all three', 'five count columns is_nullable YES with NULL default', 'second migrate() is a no-op'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/db/migrations.test.ts` → 10 pass; `npm run db:push` reports no pending changes. after: 1.4.1, 3.3.1, 3.3.2, 3.3.3
- [x] 3.4.1 Implement canonical request hash and Idempotency-Key preHandler — impl: `apps/api/src/idempotency/requestHash.ts` (`canonicalJson`: keys sorted recursively, arrays ordered, undefined dropped, null kept literal; `requestHash(operation, params, body)` = sha256 hex of `operation + '\n' + canonicalJson({ params, body })`); `keyHook.ts` `requireIdempotencyKey` preHandler for the IK routes (POST /programs, POST /sessions, recall, finalize): header must be a UUID else 400 `malformed_request` with `fieldErrors['Idempotency-Key']`; decorates `request.idempotencyKey`; unit: 8 cases incl. '{episodeCount:null}, {episodeCount:0} and {} hash differently (unknown != zero != absent)', 'array order changes the hash'; integration: 3 cases incl. 'missing header → 400', 'upper-case header name accepted'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/idempotency/hash.test.ts` → 11 pass. after: 3.2.4
- [x] 3.4.2 Implement mutation_receipts helper with 201/200 replay, mismatch 409, no receipt on failure and 7-day expiry — impl: `apps/api/src/idempotency/withIdempotency.ts` `withIdempotency(db, ctx, { key, operation, requestHash }, execute, load) → { replayed, value }` in one transaction: `pg_advisory_xact_lock(hashtext(principal:key))`, live receipt + same hash → replay via `load`, different hash → 409 `idempotency_mismatch`, absent/expired → `execute(tx)` then upsert receipt (`expires_at = ctx.now + 7 days`); any throw inside execute rolls back, so a rejected request records no receipt (D21); creators respond 201 fresh / 200 replay (D21); `isReceiptLive`; unit: 4 cases; integration: 10 cases incl. 'identical requests → 201 then 200 same id, one row', '10 concurrent identical → one row', '409 event_count_mismatch inside execute leaves no receipt and the same key then succeeds 201', 'expired receipt after demo clock +7 days is overwritten'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/idempotency/receipts.test.ts` → 14 pass. after: 3.2.3, 3.3.3, 3.4.1
- [x] 3.5.1 Implement GET /me and PATCH /me/preferences — impl: `apps/api/src/preferences.ts` (`isIanaTimeZone` via `Intl.supportedValuesOf('timeZone')` or exactly 'UTC'; `mergePreferences` keeps keys absent from the patch); `apps/api/src/routes/me.ts`: `GET /api/v1/me` → `{ principalId, identityMode, realm, timezone, preferences: { hideTimerDefault, endChime, visibilityContext, milestoneAnnouncements }, demoClockOffsetSeconds }` (D22); `PATCH /api/v1/me/preferences` body `{ timezone?, hideTimerDefault?, endChime?, visibilityContext?, milestoneAnnouncements? }` (2.7.2 contract, unknown keys 400), invalid timezone → 400 `fieldErrors.timezone`; both private no-store; unit: 6 cases incl. 'utc rejected, UTC accepted'; integration: 8 cases incl. 'GET shows UTC placeholder and DEFAULT_PREFERENCES', 'PATCH { realm: pilot } → 400 profile unchanged', 'PATCH milestoneAnnouncements true reflected'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/routes/me.test.ts` → 14 pass. after: 2.7.2, 3.2.3, 3.2.4
- [x] 3.2.5 Serve the built web app from the API under WEB_DIST_DIR — impl: `apps/api/src/plugins/staticWeb.ts` registered by buildApp after the API routes; when `config.webDistDir` is set (must contain index.html else ConfigError naming WEB_DIST_DIR) register `@fastify/static` for that directory plus an SPA fallback: any GET whose path does not start with `/api` serves index.html (deep links like `/progress`), never for `/api` paths, which keep the JSON 404 envelope; when unset, non-`/api` GETs are 404 envelopes (D37); unit: 4 cases on `isApiPath` and the missing-index guard; integration: 8 cases incl. 'deep link /progress serves index.html', '/api/v1/me still JSON', 'unknown /api path still JSON 404 envelope', 'without webDistDir /progress is a 404 envelope'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/plugins/staticWeb.test.ts` → 12 pass. after: 3.2.2, 3.2.4, 3.5.1
- [x] 3.5.2 Implement demo-only POST /demo/clock registered only in local-demo — impl: `apps/api/src/routes/demo.ts` `demoRoutes` plugin that throws ConfigError at registration unless `app.config.identityMode === 'local-demo'`, and buildApp registers it only in that mode (routes do not exist otherwise); `POST /api/v1/demo/clock` body `{ offsetSeconds: integer }` (2.7.2 `DemoClockBody`) sets an absolute `user_profiles.demo_clock_offset_seconds` (D35) → 200 `{ demoClockOffsetSeconds }`; the identity plugin reads it per request so the next `ctx.now`/`timeSource` reflect it (D8), consumed by Today (4.5.3) and the session `time_source` stamp (5.1.1); unit: 2 cases incl. 'registration in real mode throws'; integration: 8 cases incl. 'offset 1209600 → ctx.now shifted, timeSource demo_clock', 'offset 0 → measured', 'offsetSeconds 1.5 → 400'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/routes/demoClock.test.ts` → 10 pass. after: 2.7.2, 3.2.3, 3.2.4, 3.5.1
- [x] 3.5.3 Implement POST /demo/reset with principal-scoped deletePrincipalData — impl: `apps/api/src/db/seed/deletePrincipalData.ts` `deletePrincipalData(tx, ctx) → Record<string, number>` deleting only rows owned by `ctx.principalId` with `realm = ctx.realm` on the three roots (children selected through the parent join) in FK order feed_usage → daily_checkins → session_events → session_reviews → session_amendments → agent_plans → focus_sessions → benchmark_slots → (`programs.current_revision_id = NULL`) → protocol_revisions → programs → mutation_receipts, then `UPDATE user_profiles SET demo_clock_offset_seconds = 0` — the profile row, its timezone and preferences are kept (D35); exports `USER_OWNED_TABLES`; `POST /api/v1/demo/reset` runs it in one transaction → 204; log carries per-table counts only; unit: 2 cases incl. 'every schema table except user_profiles listed once'; integration: 7 cases incl. 'other-user rows untouched', 'profile kept with offset 0 and prior timezone/preferences'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/routes/demoReset.test.ts` → 9 pass. after: 3.2.3, 3.2.4, 3.3.4, 3.5.2
- [x] 3.5.4 Implement POST /demo/scenarios/{name}/load from the shared fixtures — impl: `apps/api/src/db/seed/loadScenario.ts` `loadScenario(tx, ctx, scenario)`: `deletePrincipalData` (3.5.3, zeroes the demo offset) then inserts the 2.8 fixture rows with fresh UUIDs, `user_id = ctx.principalId`, realm 'demo' on the three roots (throws on any other fixture realm), `time_source = 'demo_clock'` on sessions (D34), `baseline_date = today(program tz) − viewDay`, session `started_at = load instant − startedAtOffsetSeconds` (D35), ReportedCount fields passed through (null stays null, 0 stays 0); route `POST /api/v1/demo/scenarios/:name/load` takes no body (D22), unknown name → 404, replaces existing data → 200 `{ programId: string | null }`; unit: 3 cases incl. 'null and 0 counts never coalesced'; integration: 18 cases incl. it.each over the eight names for realm/owner scoping, 'timing-deviation loads a RUNNING baseline A started 18 min before load with an unresolved 300 s clock_gap event'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/routes/demoScenarios.test.ts` → 21 pass (13 named + 8 it.each). after: 2.8.2, 3.3.4, 3.5.3
- [x] 3.6.1 Add log-privacy assertions for validation, domain and database error paths — impl: `apps/api/test/privacy/logCapture.test.ts` over the 3.2.1 captureLogs destination; registers a test-only `POST /api/v1/__test/privacy` route (schema `{ note, points: string[5], count: integer | null, mode: 'domain' | 'db' }`) whose handler throws `DomainError('impossible_offset')` for mode 'domain' and, for mode 'db', inserts a daily_checkins row with stress 11 and the request note so Postgres raises 23514 with the row echoed in `detail`; asserts errors are logged as `{ reqId, code, statusCode, route, err: sanitizeErrorForLog }` and the only per-request entry is the 3.2.2 completion line; unit: n/a (3.2.1/3.2.2/3.2.4 cover the pure pieces); integration: 6 cases incl. 'validation failure leaks no marker', 'Postgres 23514 detail never reaches logs, sqlstate does', 'count null reaches handler as null'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/privacy/logCapture.test.ts` → 6 pass, zero marker occurrences. after: 3.2.2, 3.2.4, 3.3.4
## 4. Programs API

- [x] 4.1.1 Program service base, DTO mappers and seed helpers — impl: `programService.ts` (`loadOwnedProgram`: not owned 404, foreign realm 422 `realm_mismatch`; `governingRevisionFor` = greatest `effective_day <= max(day,0)`, ties → highest revision; `INITIAL_REVISION` {1, day 0, 'initial plan'}; `initialRevisionSettings` with `BAND_CEILINGS`; `toProgramDto`/`toRevisionDto`/`toSlotDto` with `attempts` (D22), NULL feed estimate → null never 0; `mapPgError` 23505 `programs_one_open_per_user` → 409 `program_exists`) + `test/helpers/programs.ts` seeds (insertProgram, insertSlotSet, insertSession with D31 review row, seedActiveProgram, setDemoOffsetSeconds/setDemoNow, insertOtherPrincipalProgram, insertCheckin) on 3.2.1 buildTestApp/truncateAll; unit: 9 cases incl. 'NULL feed_estimate_min maps to null, not 0', 'ties pick the highest revision', 'other 23505 rethrown'; integration: 6 cases incl. 'blank episodeCount stored NULL', 'foreign-realm program → 422'; verify: `npm run db:up && npm run test -w @attention-lab/api -- src/services/program/programService.test.ts test/helpers/programs.test.ts` → 15 passed; after: 1.5, 2.1.1, 2.5.1, 2.7.3, 3.2.1, 3.2.3, 3.2.4, 3.3.1, 3.3.2, 3.3.3
- [x] 4.1.2 POST /programs create with idempotency and realm stamping — impl: `routes/programs.ts` + `createProgram` in `programService.ts`: 3.4.1 `requireIdempotencyKey` then 3.4.2 `withIdempotency` (operation `program.create`) wraps one transaction inserting `programs` (`realm`/`user_id` from ctx never body, status draft, `leisure_allowance_min` default 20, `feed_estimate_min` NULL when omitted never 0) and revision 1 {effectiveDay 0, reason 'initial plan'} (D33); 201 first write, 200 replay (D21); `CreateProgramBody` 2.7.3 (unknown keys 400); timezone via 3.5.1 `isIanaTimeZone` → 400 `fieldErrors.timezone`; unit: 4 cases incl. 'omitted feedEstimateMinutes → null not 0', 'realm from ctx even when body says pilot'; integration: 12 cases incl. 'same key twice → 201 then 200, one row', 'Mars/Olympus → 400', 'no focus_sessions row after create'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/create.test.ts src/services/program/createProgram.test.ts` → 16 passed; after: 4.1.1, 2.7.3, 3.2.2, 3.2.4, 3.4.1, 3.4.2, 3.5.1
- [x] 4.1.3 Enforce one open program per user on POST /programs — impl: inside the 4.1.2 transaction `SELECT id, status FROM programs WHERE user_id = ctx.principalId AND status IN ('draft','baseline_ready','active') FOR UPDATE`; found → 409 `program_exists` with `details { existingProgramId, existingStatus }` (D18); a racing 23505 on `programs_one_open_per_user` maps through `mapPgError` to the same 409 with details re-queried (never 500); `isOpenStatus` exported; completed/archived and other principals never block; unit: 2 cases 'isOpenStatus true for draft|baseline_ready|active', 'false for completed|archived'; integration: 7 cases incl. 'draft exists → 409 program_exists with existingProgramId', 'archived → 201', 'two concurrent POSTs → one 201, one 409, one open row', 'someone-else open program does not block'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/create-conflict.test.ts src/services/program/programService.test.ts` → 18 passed; after: 4.1.1, 4.1.2, 3.3.1
- [x] 4.2.1 Derive today's two practice block statuses (pure) — impl: `apps/api/src/services/program/blocks.ts` `deriveBlocks({ sessions, targetSeconds }) → [Block, Block]` with Block = 2.7.3 `{ index 1|2, status, targetSeconds, sessionId|null }`; candidates = kind practice, lifecycle ≠ abandoned, ordered by startedAt, first two only (benchmarks never blocks); status: none → not_started; running|paused|awaiting_review → in_progress; finalized + completeInterval true → completed; finalized + false or NULL → partial; outputQuality never affects status; unit: 12 cases incl. 'awaiting_review past target is in_progress, never completed (timer expiry ≠ completion)', 'completeInterval null → partial', 'partly output with complete interval → completed', 'benchmark on the same date is never a block'; integration: n/a: pure function, covered by API integration in 4.2.3 and 4.5.3; verify: `npm run test -w @attention-lab/api -- src/services/program/blocks.test.ts` → 12 passed; after: 1.5, 2.7.1, 2.7.3
- [x] 4.2.2 Derive nextAction from program state (pure) — impl: `apps/api/src/services/program/nextAction.ts` `deriveNextAction({ program, day, slots (hasFinalizedAttempt), blocks })` per D23 and the spec text: null or completed|archived → setup; draft → readiness; day ≤ 13 with a baseline slot lacking a finalized attempt → `{ kind 'benchmark', slotId }` (A before B, also before Day 0); day ≥ 14 with a final slot lacking one → `{ kind 'final', slotId }`, else progress; otherwise practice with block = first in_progress, else first not_started, else 2; midpoint never; status never overrides slot state; unit: 25 cases incl. 'day 15 final B pending → final B', 'day 1 baseline B pending → benchmark B', 'day 14 baseline B and finals pending → final A', 'day 20 finals done → progress', 'abandoned attempt leaves the slot pending'; integration: n/a: pure function, covered by API integration in 4.2.3 and 4.5.3; verify: `npm run test -w @attention-lab/api -- src/services/program/nextAction.test.ts` → 25 passed; after: 1.5, 2.7.1, 2.7.3, 4.2.1
- [x] 4.3.1 Readiness validator for benchmark slot sets (pure) — impl: `apps/api/src/services/program/readiness.ts` `assignedDateFor(phase, baselineDate)` (Day 0/7/14 via `localDateForProgramDay`) and `normalizeSlotSet(slots, program) → { slots, missing, complete }`: duplicate (phase,label) → 400 `malformed_request` `fieldErrors['slots[i]']`; finals without `plannedLocalTime` inherit the same-label baseline time; both baseline times present and < 60 min apart (same-local-date minutes) → 422 `baseline_times_too_close` naming the one-hour minimum; `missing[]` = `{ phase, label, fields }` for absent rows, whitespace `materialRef`, baseline without time; midpoint optional, never missing; unit: 14 cases incl. '09:00 and 09:59 → baseline_times_too_close', '23:30 and 00:15 → ok', 'three references → missing [{final, B, [materialRef]}]', 'Day 14 date across DST 2026-10-28 → 2026-11-11'; integration: n/a: pure function, covered by API integration in 4.3.2; verify: `npm run test -w @attention-lab/api -- src/services/program/readiness.test.ts` → 14 passed; after: 1.5, 2.1.1, 2.7.3, 3.2.4
- [x] 4.3.2 PUT /programs/{id}/benchmark-slots atomic replace with freeze and status — impl: `replaceBenchmarkSlots` in one transaction: `loadOwnedProgram(forUpdate)` (404); `expectedVersion` mismatch → 409 `stale_version` `{ current }`; completed|archived → 409 `program_terminal`; 4.3.1 normalize; `isFrozen` = `frozen_at` set or any `focus_sessions.slot_id` reference; a frozen slot changed or omitted → 409 `slot_frozen` `{ phase, label }`; upsert on `UNIQUE(program_id, phase, label)`; beyond draft and incomplete → 422 `readiness_regression`; draft and complete → `baseline_ready`; version+1; 200 `{ program, slots (with attempts), missing }` (D22); never inserts a session; unit: 7 cases incl. 'session reference with null frozen_at → frozen'; integration: 14 cases incl. 'complete set on draft → baseline_ready, finals inherit times', 'attempted slot edited → 409 slot_frozen {baseline, A}', 'unattempted final edited → 200', 'active program with three refs → 422 readiness_regression'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/benchmark-slots.test.ts src/services/program/readiness.test.ts` → 35 passed; after: 4.1.1, 4.1.2, 4.3.1, 3.2.4, 3.3.1, 3.3.2
- [x] 4.2.3 GET /programs/current with day, attempts and nextAction — impl: `getCurrentProgram(ctx)` + route: open program by `user_id` and status, then `assertOwnRealm`; none → 200 `{ program: null, revision: null, slots: [], day: null, nextAction: { kind: 'setup' } }` (D22); `loadProgramSnapshot` = `currentProgramDay` (2.1.2, program tz, demo clock), `slotAttempts` → `slots[].attempts [{ sessionId, lifecycle, eligible, excludedByAmendment }]` (D22), today's practice rows by (program_id, kind, local_date), every loaded row realm-checked (422 `realm_mismatch`, never filtered), blocks 4.2.1, `governingRevisionFor`, nextAction 4.2.2; no-store; unit: 3 cases incl. 'empty snapshot has day null, not 0'; integration: 19 cases incl. 'DST 04:30Z → Day 4 then 05:30Z → Day 5', 'day 15 final A finalized → final B', 'pilot session row → 422', 'excluding amendment → excludedByAmendment true'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/current.test.ts src/services/program/programService.test.ts` → 33 passed; after: 4.1.1, 4.1.2, 4.1.3, 4.2.1, 4.2.2, 4.3.2, 2.1.2, 3.2.3, 3.2.4, 3.3.2
- [x] 4.4.1 POST /programs/{id}/revisions with required reason and effective day — impl: `revisions.ts` (`nextRevisionNumber`, `normalizeReason`, `effectiveDayAllowed` = ≥ max(currentDay,0) and ≤ 14, `composeSettings`) + `createRevision`: body per 2.7.3 `{ effectiveDay, settings { practiceTargetSeconds, leisureAllowanceMin? }, reason }`; stored settings add server-owned `bandCeilings` = `BAND_CEILINGS` and default `leisureAllowanceMin` from the program; terminal → 409 `program_terminal`; blank reason → 400 `fieldErrors.reason`; past day → 422 `effective_day_in_past`; `current_revision_id` = newest (D33); version+1; 201 `{ revision, program }`; accept path posts `PROGRESSION_ACCEPTED_REASON`; unit: 4 cases incl. 'day 8 accepts 8 and 14, rejects 7'; integration: 12 cases incl. 'Day 8 revision leaves seven Day 1–7 sessions on revision 1', 'benchmarkSeconds or bandCeilings in settings → 400', 'PATCH/DELETE on a revision → 404'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/revisions.test.ts src/services/program/revisions.test.ts` → 16 passed; after: 4.1.1, 4.1.2, 2.1.2, 2.5.2, 2.7.3, 3.2.4, 3.3.1, 3.3.2
- [x] 4.4.2 PATCH /programs/{id} with version, draft-only dates and explicit status — impl: `patch.ts` `canTransition` per D33 (completed ← baseline_ready|active; archived ← draft|baseline_ready|active; `active` is set by 5.8 when the second baseline attempt is finalized, never by PATCH), `dateEditAllowed` (draft only), `recomputeAssignedDates` via 4.3.1; route: `expectedVersion` mismatch → 409 `stale_version` `{ current }`; terminal → 409 `program_terminal`; `baselineDate` beyond draft → 422 `date_locked`, else every slot date rewritten; `leisureAllowanceMinutes` edits `programs.leisure_allowance_min` only; other transition → 409 `invalid_status_transition`; status outside completed|archived → 400; version+1; 200 `{ program }`; unit: 3 cases incl. 'draft→completed false, active→archived true'; integration: 12 cases incl. 'draft date +3 shifts every slot', 'baseline_ready date → 422 date_locked', 'leisure 30 on active leaves revisions count unchanged', 'archived frees POST /programs → 201'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/patch.test.ts src/services/program/patch.test.ts` → 15 passed; after: 4.1.1, 4.1.2, 4.3.1, 2.1.1, 2.7.3, 3.2.4, 3.3.1
- [x] 4.5.1 Progression suggestion adapter over stored sessions (pure) — impl: `apps/api/src/services/program/suggestion.ts` `toPracticeBlockRecords(rows, realm)` (every row realm must equal the request realm else `RealmMixingError` → 422; practice, non-abandoned, first two per local date by started_at; `episodeCount` null stays null, never `?? 0`; `finalized` = lifecycle finalized) and `deriveSuggestion({ day, currentTargetSeconds, rows, realm })` = day > 14 → null, else 2.5.2 `suggestProgression` → `{ suggestedTargetSeconds, qualifiedOn } | null`; no hold state stored; benchmark rows never enter; unit: 16 cases incl. 'Days 4–5 at 900 → null on Day 6, 1200 on Day 8', 'episodeCount null on one block → null (unknown ≠ zero)', 'Partly breaks the pair', 'pilot row among demo rows → RealmMixingError', 'after acceptance at 1200 → null'; integration: n/a: pure function, covered by API integration in 4.5.3; verify: `npm run test -w @attention-lab/api -- src/services/program/suggestion.test.ts` → 16 passed; after: 1.5, 2.5.1, 2.5.2, 2.7.1
- [x] 4.5.3 GET /programs/{id}/today with blocks, check-in status and suggestion — impl: `getToday(ctx, id)`: `loadOwnedProgram` (404), `loadProgramSnapshot` (4.2.3); `checkin` = shared `checkinStatus` (2.6.2; complete|incomplete|not_reported) + `values { sleepMinutes, phoneFeedMinutes, desktopFeedMinutes }` from `feedAggregates(...).feedByDevice` (2.6.1; null without a feed-scope row, 0 for an explicit zero) (D22); `suggestion` from 4.5.1 over Days 1..day−1 at the governing target, key omitted when null; `nextAction` 4.2.2; no streak or missed-day fields; no-store; unit: 3 cases incl. 'response keys exactly day/localDate/blocks/checkin/nextAction (+suggestion)'; integration: 20 cases incl. 'sleep only → incomplete, missing [feed], phone/desktop null', 'explicit-zero phone row → complete, phoneFeedMinutes 0', 'Days 4–5 qualifying → none on Day 6, 1200 on Day 8', 'Day 14 finished practice never fills a final slot'; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/programs/today.test.ts src/services/program/today.test.ts` → 23 passed; after: 4.1.1, 4.1.2, 4.2.1, 4.2.2, 4.2.3, 4.4.1, 4.5.1, 2.6.1, 2.6.2, 2.7.3, 3.2.3, 3.2.4, 3.3.3
## 5. Sessions API

- [x] 5.1.3 Sessions test seed helpers — impl: apps/api/test/helpers/sessions.ts with ONLY session-level seeds (seedSession, seedReview, seedEvents, seedAmendment, seedAgentPlan, withIdempotencyKey), reusing 3.2.1's buildTestApp/truncateAll and 4.1.1's insertProgram/insertSlotSet/seedActiveProgram/setDemoOffsetSeconds for program/slot/profile fixtures (D16 — no unit re-creates a lower layer); every count/score column seeds SQL NULL, never 0; unit: n/a — test infrastructure verified by the harness smoke test below; integration: 9 cases (defaults, review-row creation, void-safe events, amendment/agent-plan rows, partial-unique-index backstop, composed benchmark fixture); verify: `npm run test -w @attention-lab/api -- test/helpers/harness.test.ts` → 9 pass. after: 3.2.1, 4.1.1
- [x] 5.2.1 Session timing/tallies (pure) — impl: services/sessionTiming.ts deriveTiming (D5: server timestamps + open-pause freeze, no lifecycle mutation, underlies GET's D24 read-only guarantee), services/sessionTallies.ts deriveTallies (D11/D20 shape {offTask, external, agentChecks}, voided rows skipped, agent_check.alsoOffTask adds to both tallies, no cross-tally sum); unit: 13 cases (elapsed/remaining/deadline/pause math; tally dedupe, voided exclusion, hidden-tab-creates-nothing); integration: n/a, covered by 5.1.1/5.2.2/5.4.2; verify: `npm run test -w @attention-lab/api -- test/unit/sessionTiming.test.ts test/unit/sessionTallies.test.ts` → 13 pass. after: 1.2, 1.5
- [x] 5.1.1 POST /sessions practice start — impl: routes/sessions/start.ts, services/session.ts startSession, services/sessionSerializer.ts (D20 shape: serverNow, timing, tallies, eventCount, events, review, agentPlan, amendments[]); check order contract 400 → idempotency (201 first write / 200 replay, D21) → ownership 404 → active-session 409 details.activeSessionId (D18) → domain 422; time_source = ctx.timeSource (3.2.3, D8); targetSeconds any 300..1500 step 300 stored as given (D30); session_reviews row created at start (D31); unit: 7 cases (governing revision, timeSource, conditions); integration: 27 cases (replay 200, active-session 409, DST-safe local_date, concurrent-start race); verify: `npm run test -w @attention-lab/api -- test/unit/session.start.test.ts test/sessions/start.practice.test.ts` → 34 pass. after: 1.2, 1.4, 1.5, 2.1, 2.7, 3.2, 3.2.1, 3.3, 3.4, 4.1.1, 5.1.3, 5.2.1
- [x] 5.1.2 POST /sessions benchmark start — impl: services/slotAttempts.ts checkSlotStart (returns the D19 422 codes before_slot_date/slot_full/replacement_reason_required/eligible_attempt_not_retaken directly), defaultConditionsFromSlot; extends 5.1.1's startSession: slotId/intendedOutput kind-mismatch → 422 benchmark_only_field/practice_only_field; targetSeconds forced 1200 (else 422); late first start needs no reason (D23); replacementReason on a first attempt accepted but not stored; benchmark_slots.frozen_at set once on first attempt; D20 response via the shared serializer; unit: 12 cases; integration: 21 cases (slot rules, replay 200, race-free frozen_at); verify: `npm run test -w @attention-lab/api -- test/unit/slotAttempts.test.ts test/sessions/start.benchmark.test.ts` → 33 pass. after: 5.1.1, 5.1.3, 2.1, 3.3, 4.1.1
- [x] 5.2.2 GET /sessions/{id} — impl: routes/sessions/read.ts, readSession; loads events (voided incl.), review, agentPlan, amendments; responds via 5.1.1's serializeSession (D20: serverNow, timing, tallies, eventCount, events, review, agentPlan, amendments[]); ownership 404 (existence never disclosed); strictly read-only, zero writes even past deadline (D24); unit: 7 cases (null-survives-serialization, voidedAt, eventCount vs tallies, no invented aggregate); integration: 14 cases (refresh mid-practice, hidden-tab tallies, amendments visible without rewriting stored eligibility D32, no-store); verify: `npm run test -w @attention-lab/api -- test/unit/serializeSession.test.ts test/sessions/read.test.ts` → 21 pass. after: 5.1.1, 5.1.3, 5.2.1, 2.7, 3.2, 4.1.1
- [x] 5.2.3 GET /sessions/active — impl: routes/sessions/active.ts; selects the principal's single running/paused/awaiting_review session (3.3 partial unique index guarantees at most one), 204 when none, else the identical D20 shape as GET /sessions/{id} via 5.2.2's readSession (serverNow, timing, tallies, eventCount, events, review, agentPlan, amendments); registered before `/:id` so 'active' is never parsed as an id; strictly read-only (D24), no-store; unit: n/a — the route is one query plus the serializer already unit-tested in 5.2.2; integration: 9 cases (204 vs 200 per lifecycle, second-tab idempotence, eventCount/amendments scoping); verify: `npm run test -w @attention-lab/api -- test/sessions/active.test.ts` → 9 pass. after: 5.2.2, 5.1.3
- [x] 5.3.1 POST /sessions/{id}/events batch — impl: routes/sessions/events.ts, services/sessionEvents.ts (partitionBatch, findImpossibleOffsets with EVENT_OFFSET_TOLERANCE_MS=5000, withReconciliationWarning); dedupe via ON CONFLICT(session_id, client_event_id); offset beyond elapsed+5000ms → 422 `impossible_offset` naming every offending id, whole batch rejected; clock_gap events carry details{gapSeconds, resolution?} (D26, absent resolution = unresolved); finalized/abandoned sessions store late events with reconciliation_warning; pause/resume rejected by contract (400); guard test enforces services/session.ts as sole insert site; unit: 10 cases; integration: 17 cases (retry-safe counts, impossible_offset, clock_gap resolved/unresolved, hidden-tab-creates-nothing, late-event immutability); verify: `npm run test -w @attention-lab/api -- test/unit/sessionEvents.test.ts test/sessions/events.test.ts` → 27 pass. after: 5.1.1, 5.1.3, 5.2.1, 5.2.2, 2.7, 3.3, 4.1.1
- [x] 5.3.2 POST /sessions/{id}/events/{clientEventId}/void — impl: routes/sessions/void.ts, voidEvent, canVoid/isVoidableType in services/sessionEvents.ts; finalized session → 409 `already_finalized`, voided_at untouched (D9); server-written pause/resume rows → 422 `invalid_transition` (no dedicated D19 code exists, reused deliberately, see notes); already-voided → 200 idempotent; abandoned/running/paused/awaiting_review stay voidable; rows never deleted, only the 5.2.1 tallies exclude them (eventCount unaffected); unit: 4 appended cases (canVoid, isVoidableType); integration: 9 cases (undo duplicate, finalized refusal, pause-row refusal, abandoned still voidable); verify: `npm run test -w @attention-lab/api -- test/sessions/void.test.ts test/unit/sessionEvents.test.ts` → pass. after: 5.3.1, 5.2.2
- [x] 5.4.1 Session transition state machine (pure) — impl: services/sessionTransitions.ts applyTransition; running↔paused practice-only (benchmark → invalid_for_kind); end closes any open pause then sets completeInterval = unpaused elapsed ≥ target (same rule both kinds, D5/D24); abandon allowed from running, paused AND awaiting_review, explicitly including paused→abandoned (D29: abandon always available); reason is written only into a pause eventRow's details.reason, never end/abandon (D29); every pair from finalized/abandoned → invalid_from_state; no review columns touched, lifecycle never yields finalized; unit: 40-row kind×lifecycle×type matrix + 16 named cases; integration: n/a, covered by 5.4.2; verify: `npm run test -w @attention-lab/api -- test/unit/sessionTransitions.test.ts` → pass. after: 5.2.1, 1.5
- [x] 5.4.2 POST /sessions/{id}/transitions — impl: routes/sessions/transitions.ts, transitionSession; order: owned 404 → terminal (finalized/abandoned) 409 `session_not_active` before calling applyTransition → stale `expectedVersion` → 409 `stale_version` with details.current = serialized session (D18) → applyTransition failure → 422 `invalid_transition` (both kind- and state-mismatch reasons collapse here) → optimistic UPDATE (0 rows = same 409) → pause/resume insert one session_events row with details.reason from the body (pause only, D29) → 200 via 5.1.1's D20 serializer; end/abandon `reason` accepted by contract but discarded; unit: 1 appended case; integration: 21 cases (240s-paused math, stop-early leaves one row, two-tabs-end conflict, paused→abandoned, version increments); verify: `npm run test -w @attention-lab/api -- test/sessions/transitions.test.ts test/unit/sessionTransitions.test.ts` → pass. after: 5.1.1, 5.1.3, 5.4.1, 5.2.2, 5.2.3, 2.7
- [x] 5.5.1 Clock-gap route: continued, uncertain, save_incomplete — impl: `POST /sessions/{id}/clock-gap` + `resolveClockGap` in `services/session.ts`; `clock_gap_seconds` accumulates via an explicit NULL branch, never `?? 0` (D26); `save_incomplete` = 5.4's `end` path + `timer_quality='uncertain'`, `complete_interval` always false, never finalizes on its own (D25); finalized/abandoned → 409 `session_not_active`; unit: 11 cases incl. "earlier uncertain never resets to ok", "NULL clock_gap_seconds becomes gapSeconds without coalescing"; integration: 13 cases incl. "save_incomplete → awaiting_review, complete_interval false, timer_quality uncertain"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/clock-gap.test.ts test/services/clock-gap.unit.test.ts` → 13 integration + 11 unit pass. after: 2.7, 3.2, 3.3, 5.1, 5.2, 5.4
- [x] 5.6.1 Agent-plan PUT: practice only, upsert with optimistic version — impl: `PUT /sessions/{id}/agent-plan` + `putAgentPlan` upserting `agent_plans` (PK session_id); benchmark kind → 422 `practice_only`; lifecycle not running/paused/awaiting_review → 409 `session_not_active`; version mismatch → 409 `stale_version` with `details.current` (D18); `focus_sessions` row untouched; unit: 5 cases incl. "insert path requires expectedVersion 0", "stale version returns details.current"; integration: 10 cases incl. "two tabs: first 200, second 409 stale_version whose details.current equals the first write"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/agent-plan.test.ts test/services/agent-plan.unit.test.ts` → 10 integration + 5 unit pass. after: 2.7, 3.2, 3.3, 5.1, 5.2, 5.4
- [x] 5.7.2 Recall lock route: idempotent, benchmark-only, delay/overrun flags — impl: `POST /sessions/{id}/recall` + `lockRecall`; `startedAt` compared in server-aligned time (D27) via the shared `recallDelaySeconds`/`deriveRecallFlags` from `packages/shared/src/domain/recall.ts` (2.3.2, D16) — `startedAt < ended_at` or `> ctx.now` → 422 `recall_before_interval_end`; state-locked points mismatch → 409 `recall_locked`; recall on an incomplete benchmark is allowed (D25); unit: 7 cases incl. "flags come from the shared deriveRecallFlags"; integration: 19 cases incl. "early-stopped benchmark → 200", "startedAt after ctx.now → 422"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/recall.test.ts test/services/recall.unit.test.ts` → 19 integration + 7 unit pass. after: 2.3.2, 2.7, 3.2, 3.3, 3.4, 3.6.1, 5.1, 5.2, 5.4
- [x] 5.8.1 Finalize mechanics: idempotency, expected event count, atomic commit — impl: `POST /sessions/{id}/finalize` + `finalizeSession`; per D21 any rejection (400/404/409/422 incl. `event_count_mismatch`) writes NO receipt so the same key retries cleanly; `expectedEventCount` counts every stored `session_events` row incl. voided/clock_gap/visibility; mismatch → 409 `event_count_mismatch` with `details:{expected,stored}` (D18); running/paused → 409 `session_not_ended`, abandoned → 409 `session_not_active`; `idempotency_mismatch`/`already_finalized` per D19; unit: 5 cases incl. "mismatch writes no receipt (D21)"; integration: 17 cases incl. "retry the identical key after fixing the count → 200"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/finalize-mechanics.test.ts test/services/finalize.unit.test.ts` → 17 integration + 5 unit pass. after: 2.7, 3.2, 3.3, 3.4, 3.6.1, 5.1, 5.2, 5.3, 5.4
- [x] 5.8.2 Practice finalize semantics: required output quality, nullable counts, no sums — impl: practice branch of `finalizeSession`; `outputQuality` required (400 `fieldErrors.outputQuality`); counts are `ReportedCount` (absent→NULL, 0→0), `countMethod` required with `episodeCount`; benchmark-only fields → 422 `benchmark_only_field`; off-task/agent-check tallies never summed (D11); practice `observed_conditions` stays the empty value fixed at start (D31) and `eligible`/`exclusion_reasons`/`first_switch_*` stay null/empty; unit: 6 cases incl. "practice never yields an eligibility value"; integration: 13 cases incl. "agent_check alsoOffTask → offTask 1, agentChecks 1, never 2"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/finalize-practice.test.ts test/services/finalize-practice.unit.test.ts` → 13 integration + 6 unit pass. after: 5.8.1, 2.7, 5.1, 5.2, 5.3, 5.4
- [x] 5.8.3 Benchmark finalize gate: recall lock, attestation, recall scoring, reviewNote, auto-activation — impl: benchmark branch of `finalizeSession`; recall lock required only when `complete_interval` true (422 `recall_not_locked`, D25) — an incomplete benchmark finalizes without recall and eligibility (5.8.4) records `recall_missing`+`scoring_incomplete`; `recallScores` always needs a lock; scoring via shared `deriveRecallScore` (2.3.2); `outputQuality` on a benchmark → 422 `practice_only_field`; `reviewNote` (D31) stored to a new `session_reviews.review_note`; on finalizing the second baseline attempt sets `programs.status='active'` (D33); unit: 11 cases incl. "complete_interval false, no lock, no scores → proceeds"; integration: 21 cases incl. "baseline B finalized second → programs.status active", "stopped-early finalized without recall → 200"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/finalize-benchmark.test.ts test/services/finalize-benchmark.unit.test.ts` → 21 integration + 11 unit pass. after: 5.8.1, 2.3.2, 5.7.2, 4.3, 5.3
## 3. API foundation (continued)

- [x] 3.6.2 Add finalize log-privacy test proving note text never reaches Pino — impl: `apps/api/test/privacy/finalizeLogs.test.ts` driving the real routes: POST /programs (4.1.2), PUT benchmark-slots (4.3.2), POST /demo/clock to reach the slot date, POST /sessions practice with intendedOutput 'PRIVATE-OUTPUT-1b2c' (5.1.1) and benchmark (5.1.2), `end` transition (5.4.2), recall with 'PRIVATE-RECALL-5a9c' (5.7.2), finalize (5.8.1–5.8.3); no production change expected — a leak is fixed in the owning unit; unit: n/a; integration: 5 cases: 'count mismatch 409 details carry only expected/stored', 'episodeCount string → 400 fieldErrors review.episodeCount', 'benchmark finalize before recall lock → 422 recall_not_locked', 'missing materiallyDisrupted → 400 fieldErrors.materiallyDisrupted', 'successful practice finalize 200 with no marker in its completion entry'; every response and logs.text() free of all four markers; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/privacy/finalizeLogs.test.ts` → 5 pass, zero marker occurrences. after: 3.5.2, 3.6.1, 4.1.2, 4.3.2, 5.1.1, 5.1.2, 5.4.2, 5.7.2, 5.8.1, 5.8.2, 5.8.3
## 5. Sessions API (continued)

- [x] 5.8.4 Benchmark eligibility and first-switch derived by shared domain at finalize — impl: benchmark branch of `finalizeSession`; an unresolved `clock_gap` event (no `details.resolution`) forces `timer_quality='uncertain'` before eligibility (D26); builds first-switch input (off_task + agent_check{alsoOffTask}, excludes visibility/pause/resume/clock_gap/voided) via shared `deriveFirstSwitch` (2.2.2); calls shared `evaluateEligibility` (2.3.1) with `recallLockedAt`/`recallScores` raw values so an incomplete unrecalled benchmark naturally yields `recall_missing`+`scoring_incomplete` (D25) with no special-casing; auto-activation lives in 5.8.3; unit: 10 cases incl. "unresolved clock_gap forces uncertain"; integration: 21 cases incl. "stopped-early with no recall → interval_incomplete+recall_missing+scoring_incomplete"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/finalize-eligibility.test.ts test/services/finalize-eligibility.unit.test.ts` → 21 integration + 10 unit pass. after: 5.8.3, 2.2.2, 2.3.1, 5.5.1, 3.5, 4.3, 5.1, 5.3
- [x] 5.9.1 Amendments route: append-only reason with exclude flag — impl: `POST /sessions/{id}/amendments` + `addAmendment`; per D32, accepted on a finalized session of either kind (201), lifecycle must be `finalized` else 409 `not_finalized`, kind never checked; append-only `session_amendments` row, no update/delete route; `amendments[]` (already part of the D20 session shape) populated in `serializeSession` in `created_at` order so `GET /sessions/{id}` lists both attempts; reason text never logged (3.6); unit: 4 cases incl. "addAmendment issues a single INSERT and no UPDATE"; integration: 9 cases incl. "after both amendments eligible/exclusion_reasons are byte-identical to before"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/amendments.test.ts test/services/amendments.unit.test.ts` → 9 integration + 4 unit pass. after: 2.7, 3.2, 3.3, 3.6.1, 5.1, 5.2, 5.8.1
- [x] 5.9.2 Shared amendment-exclusion overlay on stored eligibility — impl: `applyAmendmentExclusion` in `packages/shared/src/domain/eligibility.ts`; any excluding amendment → `eligible:false` + `excluded_by_amendment` appended once after the stored reasons; `eligible:null` (practice) stays null; no other stored field touched; used by 5.9.3's report mapper and the 5.1 replacement rule; unit: 6 cases incl. "eligible null (practice) stays null"; integration: n/a, pure function covered by 5.9.3; verify: `npm run test -w @attention-lab/shared -- src/domain/eligibility.test.ts` → the six overlay cases pass alongside 2.3.1's cases and typecheck is clean. after: 2.3.1
## 6. Days, report, export and research API

- [x] 6.1.1 Implement GET days route and the check-in view builder — impl: `apps/api/src/services/checkinView.ts` `buildCheckinView(row|null, feedRows, localDate)` returning the D22 `{ checkin, feed, status, aggregates, version }` shape; status only from shared `checkinStatus` (2.6.2), aggregates only from `feedAggregates` (2.6.1); no row → 200 shell (all-null checkin, feed [], `not_reported` missing ['sleep','feed'], version 0); null never becomes 0; `platform 'all'` rows are device totals (D36); `apps/api/src/routes/days.ts` GET via `loadOwnedProgram` (404), date outside Days 0–14 → 404, malformed date → 400 `malformed_request`, no-store; unit: 8 cases incl. `no row → not_reported shell version 0`, `desktop 30 no phone → feedByDevice.phone null, partial true`, `phone 0 → 0 not null`; integration: 11 (inject, rows via Drizzle) incl. empty shell, app_total excluded, pilot row → 422 `realm_mismatch`, other principal → 404; verify: `npm run db:up && npm run test -w @attention-lab/api -- days.get checkinView` → 19 passed; after: 1.4, 1.5, 2.1.1, 2.6.1, 2.6.2, 2.7.5, 3.2.2, 3.2.3, 3.2.4, 3.3.3, 4.1.1
- [x] 6.1.2 Implement PUT days atomic write path with version conflict — impl: `apps/api/src/routes/days.ts` PUT with the 2.7.5 `PutDayBody` (`additionalProperties: false`, `realm` → 400); `apps/api/src/services/checkin.ts` `saveCheckin` in ONE transaction: `loadOwnedProgram` (404), terminal program → 409 `program_terminal`, date outside Days 0–14 → 404, feed.ts validation mapped to 422 `feed_subset_violation` / `duplicate_feed_row` / `feed_platform_conflict` (D36 `all`-vs-platform rows), `expectedVersion` vs stored version (0 when no row, D22) → 409 `stale_version` with `details.current` = current view (D18), upsert stamping `realm = ctx.realm`, absent fields NULL never 0, atomic replace of feed rows, explicit `minutes: 0` kept, empty save allowed, returns `buildCheckinView`; unit: 7 cases incl. `absent fields → null`, `phone all + phone instagram → feed_platform_conflict`; integration: 21 incl. create with v0, stale save returns first values, 422 body has no note text, Day 10 without Day 9 → 200, completed program → 409; verify: `npm run db:up && npm run test -w @attention-lab/api -- days.put checkin.write` → 28 passed; after: 6.1.1, 2.1.1, 2.6.1, 2.7.5, 3.2.3, 3.2.4, 3.3.3, 4.1.1
- [x] 6.2.1 Implement report core: attempts, samples, realm guard, comparison — impl: `apps/api/src/services/report/attempts.ts` `loadAttemptSummaries` (slots LEFT JOIN benchmark sessions, reviews, amendments; rows loaded by ownership, never filtered by realm; `assertSameRealm` over program + attempts → 422 `realm_mismatch`, fixed message, no partial body); each attempt is the widened `AttemptSummary` (2.7.1) plus `lifecycle, replacementReason, excludedByAmendment`; NULL counts stay null; `unknown` never becomes `none_capped`; amendments overlaid via shared `applyAmendmentExclusion` (5.9.2); never-finalized sessions listed with `eligible false`, `exclusionReasons []`, lifecycle shown; midpoint listed, never counted; `buildReport` uses `selectSlotCandidates`/`sampleCounts`/`computeComparison`/`resolveResultState` from comparison.ts — the API computes no S0/S14/percentage itself; `GET /programs/:id/report` → 2.7.5 `ReportResponse`, no-store; unit: 8 cases incl. `NULL episode_count → null`, `awaiting_review past target → ineligible, not counted`, `abandoned final → finalAttemptsExist false`; integration: 12 incl. capped T → no mean, running baseline A → baseline_pending, pilot row → 422, both revisions listed; verify: `npm run db:up && npm run test -w @attention-lab/api -- report.core report.attempts` → 20 passed; after: 1.4, 1.5, 2.1.2, 2.4.1, 2.4.2, 2.7.1, 2.7.5, 2.8.1, 2.8.2, 3.2.3, 3.2.4, 3.3.2, 3.5.4, 4.1.1, 5.9.2
- [x] 6.2.2 Add report acceptance table for PRD §6 scenarios and extra states — impl: `apps/api/test/report.scenarios.test.ts` + `apps/api/test/helpers/reportVariants.ts`: table-driven over the eight scenario names: `POST /demo/scenarios/{name}/load` (no body, D22) then `GET /programs/{id}/report`, asserting `resultState` equals the fixture's `expected.resultState` (D35); new-user returns `programId null` so the test creates an empty program via `POST /programs`; four extra row sets derived from comparable-change by editing `session_reviews` via Drizzle (more switches 3,3→5,5; unchanged; low baseline 2,2→1,1; zero beats direction 0,0→1,1); copy asserted against `RESULT_STATE_COPY` and `CAUSE_NOTE` from comparison.ts (2.4.2); no key matching /score|confidence|significance|pValue/i other than `recallScore`, no 'attention +' text, `realm 'demo'` everywhere; unit: n/a — this is a test-only acceptance suite (D14); integration: 14 cases incl. `timing deviation → baseline_pending with running baseline A and zero finalized attempts`, `working day → final_pending`, `recovery → final_pending`, `zero baseline → no Infinity/NaN/100`; verify: `npm run db:up && npm run test -w @attention-lab/api -- report.scenarios` → 14 passed; after: 6.2.1, 2.4.2, 2.8.1, 2.8.2, 3.5.4, 4.1.2
- [x] 6.2.3 Add comparability warnings to the report — impl: `apps/api/src/services/report/warnings.ts` `reportWarnings(attempts)` = `comparabilityWarnings(selectSlotCandidates(attempts))` from comparison.ts (2.4.3/2.4.1) mapped to the 2.7.5 `{ label, field, baseline, final, message }` shape; compares only the eligible baseline and final attempt of each label (the comparison's real inputs, D7.5); a label lacking an eligible attempt on either side emits nothing; null vs value counts as a difference reported as not recorded; accommodations compared as sets; warnings never read or write `eligible`/`exclusionReasons` and never suppress `comparison`; wired into `buildReport` as `warnings[]`; unit: 5 cases incl. `only eligible attempts are compared (excluded-by-amendment attempt ignored)`, `language en vs es on B → one warning`, `inputs unchanged after the call`; integration: 3 incl. `final B language changed via Drizzle → warning for B and comparison still present`, `accommodation added on final A → warning, eligible unchanged`; verify: `npm run db:up && npm run test -w @attention-lab/api -- report.warnings` → 8 passed; after: 6.2.1, 2.4.1, 2.4.3
- [x] 6.2.4 Add practice and daily sections with gaps to the report — impl: `apps/api/src/services/report/practice.ts` (practice sessions only, benchmarks never; LEFT JOIN reviews; entry per 2.7.5 plus additive `sessionId, programDay, lifecycle, completedSeconds, timerQuality, countMethod, mindWanderingCount`; `completedSeconds = ended_at − started_at − paused_seconds` else null; every count a `ReportedCount`; off-task and agent-check tallies never summed, D11) and `apps/api/src/services/report/days.ts` (programDay 0..min(today's day, 14), `checkin: null | buildCheckinView(...)` — a missing day is null, never a zero record); practice rows and check-ins join the 6.2.1 same-realm check (422 `realm_mismatch`, no silent filtering); wired as `practice[]`/`days[]`; unit: 7 cases incl. `240 s paused excluded; null when ended_at null`, `episode_count 3 + unplanned_agent_checks 2 → no field equals 5`; integration: 6 incl. `targets 600/900/1500 ascending and comparison unchanged`, `Day 9 missing → days[9].checkin null`, `practice on Day 14 → in practice[], comparison unchanged`, `pilot check-in → 422`; verify: `npm run db:up && npm run test -w @attention-lab/api -- report.sections` → 13 passed; after: 6.2.1, 6.1.1, 2.1.1, 2.1.2, 2.6.1, 2.6.2, 2.7.5, 3.3.2, 3.3.3
- [x] 6.2.5 Add report load script gating p95 under one second — impl: `apps/api/scripts/report-load.ts` (run with tsx) against `DATABASE_URL_TEST`: seeds one demo program with 4 finalized benchmark attempts, 28 finalized practice sessions and 100 000 `session_events` rows (batched inserts of 5 000), issues 20 `GET /api/v1/programs/{id}/report` calls through Fastify inject, prints `report p95 <ms> ms`, exits 1 when p95 > 1000 (design.md NFR: report < 1 s at 100k events), deletes its rows; script `load:report` in apps/api/package.json; verify: `npm run db:up && npm run load:report -w @attention-lab/api` → prints `report p95 <n> ms` with n < 1000 and exits 0; after: 6.2.4
## 5. Sessions API (continued)

- [x] 5.9.3 Honor amendment exclusions in the report and the replacement rule — impl: `report.ts` attempt mapper applies `applyAmendmentExclusion` (5.9.2) to `eligible`/`exclusionReasons` only, every other `AttemptSummary` field passes through untouched; `session.ts`'s replacement check (5.1) applies the same overlay so an excluded eligible attempt is replaceable and an unamended eligible one still 422s; null counts pass through as null; unit: 3 cases incl. "replacement check still refuses an eligible unamended attempt"; integration: 5 cases incl. "excluded eligible baseline A → 201 replacement start; without the amendment the same start → 422"; verify: `npm run db:up && npm run test -w @attention-lab/api -- test/sessions/amendment-exclusion.test.ts test/services/report-amendments.unit.test.ts` → 5 integration + 3 unit pass. after: 5.9.1, 5.9.2, 5.8.4, 5.1, 6.2
## 6. Days, report, export and research API (continued)

- [x] 6.3.1 Implement GET export format=csv — impl: `apps/api/src/services/export/model.ts` `buildExportModel` reusing `loadAttemptSummaries` (6.2.1), the days loader (6.2.4) and `protocol_revisions`, `assertSameRealm` at entry → 422 `realm_mismatch`; sections `program`, `revisions`, `attempts` (progress-spec columns: `attempt_id, phase, label, local_date, realm, time_source, s, s_method, t_state ('20+, capped'|'known'|'Unknown'|''), t_seconds, t_method, recall_score, e, m, disruption, device_format, language, material_level, accommodations, eligible, exclusion_reasons, protocol_revision, recall_flags, replacement_reason, lifecycle`), `days`, `feed_rows`; free text (note, output_note, disruption_note, recall points, review_note) and practice sessions are not exported; null → empty cell, 0 → '0'; `apps/api/src/services/export/csv.ts` RFC 4180 quoting, demo label FIRST line, `# <section>` line before each section; route `GET /programs/:id/export?format=csv` → `text/csv; charset=utf-8`, no-store, 404 not owned, 400 invalid format; unit: 6 cases incl. `null → empty, 0 → '0'`, `unknown → 'Unknown' never '20+'`, `no free-text cell`; integration: 7 incl. UUID-normalized snapshot, blank-count fixture `s empty, exclusion_reasons contains count_unknown`, first line demo label; verify: `npm run db:up && npm run test -w @attention-lab/api -- export.csv` → 13 passed; after: 6.2.1, 6.2.4, 2.2.2, 2.7.5, 2.8.1, 3.2.2, 3.5.4
- [x] 6.3.2 Implement GET export format=markdown — impl: `apps/api/src/services/export/markdown.ts` over the 6.3.1 export model: FIRST line the demo label when `ctx.realm === 'demo'`, then `# Attention Lab program record`, program metadata table, revisions table, attempts table with the same columns as the CSV (S with method, T with state incl. '20+, capped' and 'Unknown', recall score, E, M, disruption, conditions, eligibility and exclusion reasons, protocol revision, recall flags, lifecycle) captioned self-reported, days table, feed-rows table; free text and practice sessions omitted; null → empty cell, `|` escaped as `\|`; route serves `format=markdown` → `text/markdown; charset=utf-8`, no-store; unit: 5 cases incl. `pipe escaped`, `null → empty`, `'20+, capped' visible`, `no free-text cell`; integration: 5 incl. UUID-normalized snapshot, blank-count fixture, first line demo label, header carries every required column; verify: `npm run db:up && npm run test -w @attention-lab/api -- export.markdown` → 10 passed; after: 6.3.1
- [x] 6.4.1 Implement GET /research/cards from the shared fixture — impl: `apps/api/src/routes/research.ts` `registerResearchRoutes(app, { cards = RESEARCH_CARDS })`: `GET /api/v1/research/cards` returns the 2.7.5 `ResearchCardsResponse` `{ cards (exactly 3 from 2.8.3: Castelo 2025, Mrazek 2013, Leroy & Glomb 2018), curatedOn: '2026-09-06', note: RESEARCH_NOTE, discoveryNote: 'Automated discovery not enabled' }`; route declared `config: { public: true }` (the one route without no-store, 3.2.2); at registration every `sourceUrl` must match `/^https?:\/\//` else boot throws; no DB, no pagination, no rotation — byte-identical responses; unit: 2 cases (`fixture has exactly 3 cards with every field and https sourceUrls`, `a javascript: sourceUrl makes registration throw`); integration: 3 (`200 with 3 cards, curatedOn, note, discoveryNote`, `two GETs byte-identical`, `no Cache-Control: no-store on this public route`); verify: `npm run db:up && npm run test -w @attention-lab/api -- research` → 5 passed; after: 1.5, 2.7.5, 2.8.3, 3.2.2, 3.2.4
## 7. Web foundation

- [x] 7.1.1 Scaffold web app (Router, Query, Tailwind, Radix, dev proxy) and create the shared component-test harness once here (D16, absorbing 8.1.1/8.6.1) — component: Providers, Root, router.tsx (rail/session layout routes), ScreenPlaceholder, NotFound, RouteError, plus test/{setup,renderWithProviders,mockClient,fakeOutbox,expectNoIdentifiers}.tsx; state: none, placeholder QueryClient only; api: dev proxy forwards `/api/*` to 127.0.0.1:8787, mockClient stubs every 7.4.1 method; validation: none (route resolution + harness smoke) · `router.test.tsx`+`harness.test.tsx`, 7 cases; verify: `npm run typecheck -w @attention-lab/web && npm run test -w @attention-lab/web -- src/app/router.test.tsx src/test/harness.test.tsx && npm run build -w @attention-lab/web` → 7 passed, `dist/index.html` exists — after: 1.1, 1.2
- [x] 7.1.6 Accessibility baseline primitives, reduced motion, live-region policy, copy guard — component: Button, VisuallyHidden, LiveRegion+useAnnouncer(), usePrefersReducedMotion(), useMilestoneAnnouncements({remainingSeconds, enabled, milestonesSeconds}) whose enabled comes from preferences.milestoneAnnouncements (default false, D22) and whose production milestones are [300,0] (D39); state: live-region text (single polite region); reduced-motion match; api: none; validation: none · Button.test.tsx+reducedMotion.test.ts+milestones.test.tsx+copyGuard.test.ts, 7 cases; verify: `npm run test -w @attention-lab/web -- src/ui src/lib/a11y src/test/copyGuard.test.ts` → 7 passed — after: 7.1.1
- [x] 7.2.1 lib/clock: remaining-time derivation from server fields and monotonic delta (D5) — component: none, pure src/lib/clock/remaining.ts; state: none, callers hold the ClockAnchor; api: consumes timer fields and the `serverNow` ISO field (D20) from GET /sessions/active and GET /sessions/{id}; validation: none · remaining.test.ts, 9 cases (remaining after refresh, paused seconds excluded, frozen while paused, deadline reached with display deadline_reached, no `completed` display value, Date.now-independence, elapsedMsForEvent, serverNowMs, formatRemaining edges); verify: `npm run test -w @attention-lab/web -- src/lib/clock/remaining.test.ts` → 9 passed — after: 7.1.1
- [x] 7.2.2 lib/clock: 5-second heartbeat gap detector with 60-second threshold, drift-only signal (D5) — component: none, pure src/lib/clock/gapDetector.ts; state: internal base timestamps only; api: none (the consumer posts POST /sessions/{id}/clock-gap in 8.10); validation: none · gapDetector.test.ts, 8 cases (drift-free run, +300 s gap, 59 s no-gap boundary, 61 s gap, rebase after a gap, clock set back -120 s, stop(), hidden tab produces no gap and no side effect); verify: `npm run test -w @attention-lab/web -- src/lib/clock/gapDetector.test.ts` → 8 passed — after: 7.1.1
- [x] 7.2.3 useSessionClock hook: ticking re-derivation plus gap detection, re-anchored on the D20 serverNow field — component: hook useSessionClock(input); state: anchor ref (serverNowAtLoadMs = Date.parse(serverNow)), 1 s tick counter, pending gap state; api: reads fields + serverNow from useActiveSession/useSession, makes no requests; validation: none · useSessionClock.test.tsx, 7 cases (90 s advance, no accumulation under throttled ticks, paused freezes remaining, deadline_reached with no completed value, gap detection + acknowledgeGap(), hidden tab creates no gap, null input runs no interval/detector); verify: `npm run test -w @attention-lab/web -- src/lib/clock/useSessionClock.test.tsx` → 7 passed — after: 7.2.1, 7.2.2
- [x] 7.3.1 lib/outbox: IndexedDB store with expiry, local-write failure path, and boot-time purge-by-exclusion (D17) — component: none, module src/lib/outbox/store.ts exporting openOutbox, enqueue, listUnsent, ack, purgeSession, purgeOtherSessions, expireOlderThan, OutboxWriteError; state: IndexedDB store 'events' only; api: none; the record shape matches the events contract; validation: none · store.test.ts, 9 cases (UUID assigned before the put resolves, per-session ordering, ack removes only given ids, purgeSession scoped correctly, purgeOtherSessions removes every other session's rows and all rows given null, 7-day expiry boundary, failing put / missing indexedDB both reject OutboxWriteError, stored keys never include note/token/requestId/preference fields); verify: `npm run test -w @attention-lab/web -- src/lib/outbox/store.test.ts` → 9 passed — after: 7.1.1, 2.7
- [x] 7.4.1 Typed API client from shared TypeBox contracts with error mapping — component: none, modules lib/api/{client,routes,errors,newIdempotencyKey}.ts exporting api, newIdempotencyKey, the ApiError hierarchy; state: stateless, idempotency keys owned by callers; api: every design.md route one-to-one; 400/422->ValidationError, 404->NotFoundError, 409->ConflictError (full body retained, D18), 429->RateLimitError, >=500->ServerError, network/timeout->NetworkError; validation: bodies typed from shared contracts so realm or an unknown field is a compile error · client.test.ts (8 cases) + client.test-d.ts (1 compile-time case); verify: `npm run test -w @attention-lab/web -- src/lib/api && npm run typecheck -w @attention-lab/web` → 8 passed, typecheck 0 — after: 7.1.1, 2.7, 3.2
- [x] 7.3.2 lib/outbox: batched flush, replay on load, direct send — component: none, module src/lib/outbox/flush.ts exporting flush, replayOnLoad, sendDirect; state: in-flight promise map per sessionId; api: POST /sessions/{id}/events, <=100/batch; accepted[]/duplicates[] both acked; NetworkError keeps rows; 422 keeps rows and reports the offending clientEventId (resolution left to 8.5, not covered by D16-D40); validation: none · flush.test.ts, 7 cases (250-row chunking, retry re-sends and dedupes via duplicates[], network error on batch 2 keeps it, 422 keeps rows + offending id, concurrent calls collapse to one POST, sendDirect writes nothing to IndexedDB, no ack before the response); verify: `npm run test -w @attention-lab/web -- src/lib/outbox/flush.test.ts` → 7 passed — after: 7.3.1, 7.4.1
- [x] 7.3.3 Sync-state derivation (Pending/Saved/Could-not-save) and useOutbox hook — component: hook useOutbox(sessionId, api); pure deriveSyncState and SYNC_STATE_COPY; state: reducer of recorded events, voided set, unsent/inFlight/localWriteFailures, derived tallies; api: flush/replayOnLoad (7.3.2) after each record, on mount, and via retry(); sendDirect via sendDirectNow(); validation: none (drafts are typed) · syncState.test.ts+useOutbox.test.tsx, 10 cases (synchronous tally on record(), pending while in-flight, saved only after ack, could_not_save + sendDirectNow(), alsoOffTask tallying per D11 with no combined key, pause/resume/clock_gap/visibility tally nothing, markVoided, mount-time replayOnLoad, no UUID in copy); verify: `npm run test -w @attention-lab/web -- src/lib/outbox/syncState.test.ts src/lib/outbox/useOutbox.test.tsx` → 10 passed — after: 7.3.2
- [x] 7.3.4 finalizeWithSync and abandonWithPurge helpers — component: none, module src/lib/outbox/finalize.ts exporting finalizeWithSync, abandonWithPurge; state: none beyond outbox rows; the idempotency key is owned by the caller; api: POST /sessions/{id}/finalize (IK) with expectedEventCount/lastBatch (D21); event_count_mismatch -> flush then ONE same-key retry with expectedEventCount = stored + flush.acceptedCount, lastBatch []; POST /sessions/{id}/transitions {type:'abandon'}; validation: none · finalize.test.ts, 6 cases (mismatch-then-retry-then-purge, NetworkError rethrows and a later call reuses the key, success purges and returns the server payload unchanged, non-mismatch failure purges nothing, 130-row pre-flush chunks 100+30, abandon posts and purges); verify: `npm run test -w @attention-lab/web -- src/lib/outbox/finalize.test.ts` → 6 passed — after: 7.3.2, 7.4.1
- [x] 7.4.2 Query keys, session-mode provider, session-aware refetch policy, boot-time outbox replay and purge (D15, D17) — component: SessionModeProvider, useSessionMode/useMe/useActiveSession/useSession; state: TanStack cache + a session-mode ref derived from sessions.active's lifecycle, plus a one-shot boot-replay guard; api: GET /me, /sessions/active, /sessions/{id}; on the first sessions.active resolution calls 7.3.2 replayOnLoad for the active session then 7.3.1 purgeOtherSessions (or purges everything when none is active) — no redirect, screens decide what to render; validation: none · policy.test.tsx+hooks.test.tsx+sessionMode.test.tsx, 11 cases; verify: `npm run test -w @attention-lab/web -- src/lib/query/policy.test.tsx src/lib/query/hooks.test.tsx src/lib/query/sessionMode.test.tsx` → 11 passed — after: 7.4.1, 7.3.1, 7.3.2
- [x] 7.1.2 AppBootstrap and DemoBanner rendered from GET /me — component: AppBootstrap({children}), DemoBanner, MeContext/useMeContext(); state: queryKeys.me pending/error/success gates rendering; MeContext value; api: GET /me via useMe(); identityMode drives banner visibility; validation: none · DemoBanner.test.tsx+AppBootstrap.test.tsx, 6 cases (banner text + aria-label + no live role in local-demo, absent otherwise, children gated until /me resolves, error shows Retry, no identifiers printed); verify: `npm run test -w @attention-lab/web -- src/app/DemoBanner.test.tsx src/app/AppBootstrap.test.tsx` → 6 passed — after: 7.4.1, 7.4.2
- [x] 7.1.3 RailLayout: four destinations, rail on desktop, bottom nav on phones — component: RailLayout, NavItem({to,label,icon}), useMediaQuery(query); state: media-query match (local); router location for aria-current; api: none beyond DemoBanner's GET /me and the provider's GET /sessions/active; validation: none · RailLayout.test.tsx, 6 cases (four links with design.md hrefs, skip-link tab order, aria-current on the active route, rail vs bottom nav by breakpoint, banner rendered before nav, exactly one main landmark); verify: `npm run test -w @attention-lab/web -- src/app/layouts/RailLayout.test.tsx` → 6 passed — after: 7.1.2
- [x] 7.1.4 SessionLayout with no navigation for benchmark, focus, review routes — component: SessionLayout (no props; Outlet); state: none; api: none beyond DemoBanner's GET /me and the provider's GET /sessions/active; validation: none · SessionLayout.test.tsx, 4 cases (no navigation landmark at /focus/s1, no links to any rail destination, DemoBanner visible on every session path, all five session paths render inside SessionLayout); verify: `npm run test -w @attention-lab/web -- src/app/layouts/SessionLayout.test.tsx` → 4 passed — after: 7.1.2
- [x] 7.1.5 Playwright shell smoke test plus the reusable support helpers (D16, absorbing 9.1.1's demo fixture and 9.1.2's clock/copy/a11y helpers) — impl: e2e/smoke/shell.spec.ts against dev API (dev:api) + dev web (dev:web, Vite proxy) asserting the demo banner and rail/bottom nav on desktop and mobile with no horizontal scroll, and no nav landmark on a session route; e2e/support/demo.ts (reset,load,setClock absolute,offset,current,today,active,session,report,readyProgram), clock.ts (installClock,advance relative,sleep,hideTab/showTab), copy.ts (expectNoPunitiveCopy,expectDemoBanner,expectNoTechnicalIds), a11y.ts (expectNoSeriousViolations,expectFocusVisible) — server-free to typecheck; verify: `npm run typecheck:e2e` → 0, then `npx playwright test -c e2e/playwright.config.ts e2e/smoke/shell.spec.ts` → 3 passed — after: 7.1.3, 7.1.4, 1.3, 1.4, 1.5, 2.7, 3.5
- [x] 7.3.5 useFinalizeSession hook: wraps finalizeWithSync/abandonWithPurge with a three-mismatch cap (D16, absorbing 8.10.5) — component: hook useFinalizeSession(sessionId) in lib/outbox/useFinalizeSession.ts; state: one Idempotency-Key persisted in sessionStorage under finalize:{sessionId} (the only browser-storage write besides IndexedDB), mismatch counter capped at 3, status idle/flushing/submitting/mismatch/unsaved_entries/error/success; api: calls only 7.3.4's finalizeWithSync/abandonWithPurge, retrying event_count_mismatch up to 3x with the same key then reporting unsaved_entries; validation: same key across retries, success only on a real 200 · useFinalizeSession.test.tsx, 7 cases; verify: `npm run test -w @attention-lab/web -- src/lib/outbox/useFinalizeSession.test.tsx` → 7 passed — after: 7.3.4, 7.4.1, 7.4.2
- [x] 7.4.3 Route-leave guard warning when a session is active, copy final per D38 — component: SessionLeaveGuard (no props) using useBlocker, Radix AlertDialog, ui/Button; state: blocker state from React Router; session-mode ref from 7.4.2; api: none, asserted by spies that no session route is called; validation: none · SessionLeaveGuard.test.tsx, 6 cases; verify: `npm run test -w @attention-lab/web -- src/app/SessionLeaveGuard.test.tsx` → 6 passed — after: 7.4.2, 7.1.6
- [x] 7.4.4 useStartSession hook: THE start hook for Today and Benchmark ready (D16, absorbing 8.10.6), one Idempotency-Key per logical start — component: hook useStartSession(); state: key minted per distinct body (deep-equal; a changed body mints a fresh key), status idle/pending/error/success, activeSessionId from a 409's details; api: POST /sessions (IK); 409 active_session_exists reads details.activeSessionId per D18 and invalidates sessions.active; 422 exposes fieldErrors; NetworkError retries the same key twice then surfaces error; validation: none, intended-output rules live in 8.2 · useStartSession.test.tsx, 6 cases; verify: `npm run test -w @attention-lab/web -- src/lib/query/useStartSession.test.tsx` → 6 passed — after: 7.4.1, 7.4.2
## 8. Web screens

- [x] 8.1.2 PlanForm — component: PlanForm, TimezoneConfirm; state: controlled form fields, `confirmed` flag, stable Idempotency-Key ref, createProgram mutation; api: `POST /programs` (IK) — 400 fieldErrors per field, 409 open-program → route via `GET /programs/current` (draft→readiness, else today), network → inline retry; validation: date required, IANA timezone + confirm checked, duration ∈ {5,10,15}, leisure ≥0, feed estimate blank-or-≥0 never coerced to 0, no bandCeilings sent · PlanForm.test.tsx, 9 cases: omits blank feedEstimateMinutes, blocks save until timezone confirmed, sends no bandCeilings for 5-min duration, 409 routing, network retry reuses key; verify: `npm run test -w @attention-lab/web -- src/features/setup/PlanForm.test.tsx` → 9 pass. after: 7.1.1, 7.1, 7.4, 2.7
- [x] 8.1.3 ReadinessForm — component: ReadinessForm, SlotRow, MissingSlots; state: rows keyed by phase+label, `finalsTouched`, `missing` list, putBenchmarkSlots mutation; api: `GET /programs/current`, `PUT /programs/{id}/benchmark-slots` — 409 frozen/stale, 422 one-hour rule; validation: materialRef non-empty, times HH:MM ≥60 min apart, frozen rows read-only · ReadinessForm.test.tsx, 9 cases: missing-slot list, one-hour rejection, frozen read-only, status !== draft → /today, stale-version notice; reachable mid-program via Settings' 'Edit benchmark materials' link (D38), not only from setup; verify: `npm run test -w @attention-lab/web -- src/features/setup/ReadinessForm.test.tsx` → 9 pass. after: 7.1.1, 7.1, 7.4, 2.7
- [x] 8.2.1 Today frame — component: Today, NextAction; state: queries `['programs','current']`, `['today', programId]`; api: `GET /programs/current` (setup/404→/setup), `GET /programs/{id}/today`; validation: none (no inputs) · Today.test.tsx, 8 cases: one primary control per nextAction, new-user has no %/improv text, missed-day copy has no streak/lost/broken/restart/behind, Day 14 still shows two blocks, server `day` used over browser date, four nav destinations reachable, Retry on fetch failure; verify: `npm run test -w @attention-lab/web -- src/features/today/Today.test.tsx` → 8 pass. after: 7.1.1, 7.1, 7.4, 2.7
- [x] 8.1.4 Playwright P-01 — impl: e2e/setup-p01.spec.ts resets demo data via e2e/support/demo.ts's `resetDemo` (7.1.5), runs Setup→Readiness, asserts /today, 'Start with your baseline', `GET /sessions/active` 204, `programs/current` status baseline_ready and feedEstimateMinutes null (never 0), no %/attention+ text via e2e/support/copy.ts; exports `completeSetup(page, request)` from e2e/helpers/setup.ts built on the 7.1.5 primitives; verify: `npm run build && npx playwright test e2e/setup-p01.spec.ts` → '1 passed'. after: 8.1.2, 8.1.3, 8.2.1, 7.1, 7.1.5, 4.1, 4.2, 4.3, 5.2, 3.5, 1.5
- [x] 8.2.2 BlockCard — component: BlockCard, StartPracticeForm; state: controlled intendedOutput; useStartSession (7.4.4) owns the mutation, Idempotency-Key and status, no local key handling; api: `useStartSession().start` → `POST /sessions` (IK) — 400 fieldErrors.intendedOutput, active_session_exists handled entirely by 7.4.4 + Today's ActiveSessionCard (8.2.5, BlockCard does no 409 routing), network → hook's error status; validation: intendedOutput 1–200 chars, no slotId ever sent · BlockCard.test.tsx, 10 cases: empty/201-char output, body shape, navigates only after resolve, active_session_exists leaves BlockCard inert, network retry, four status badges, Day 4/Day 14 targets; verify: `npm run test -w @attention-lab/web -- src/features/today/BlockCard.test.tsx` → 10 pass. after: 8.2.1, 7.1.1, 7.4, 7.4.4, 2.7
- [x] 8.2.3 CheckinCard — component: CheckinCard; state: no query of its own, reads the parent Today `['today', programId]` cache; api: none directly — status, missing[] and values (sleepMinutes, phoneFeedMinutes, desktopFeedMinutes; nulls preserved) all come from `today.checkin.values` per D22, no request to `GET /programs/{id}/days/{date}`; validation: none (display only) · CheckinCard.test.tsx, 6 cases: missing sleep shows not-yet-reported not 0, complete check-in shows all three values, phone-missing/desktop-reported mix, explicit-zero vs null, no days-route request, link targets `/checkin/:localDate`; verify: `npm run test -w @attention-lab/web -- src/features/today/CheckinCard.test.tsx` → 6 pass. after: 8.2.1, 7.1.1, 7.4, 2.7
- [x] 8.2.4 SuggestionBanner — component: SuggestionBanner; state: createRevision mutation, `held` boolean seeded from sessionStorage; api: `POST /programs/{id}/revisions` (reason 'progression accepted', practiceTargetSeconds +300), 409 stale → refetch `programs/current`; validation: no free-text inputs · SuggestionBanner.test.tsx, 7 cases: no suggestion → no banner, Accept posts +300 and invalidates today, Hold posts nothing and Start afterwards still uses the current (900, not 1200) target, sessionStorage-throws is non-fatal, no streak/unlock/level copy; verify: `npm run test -w @attention-lab/web -- src/features/today/SuggestionBanner.test.tsx` → 7 pass. after: 8.2.1, 8.2.2, 7.1.1, 7.4, 2.7
- [x] 8.2.5 ActiveSessionCard — component: ActiveSessionCard (props: session, staleNotice?); state: `['sessions','active']` query (Today, Ready) or the caller's `['sessions', id]` cache (session screens); api: `GET /sessions/active` (200|204); consumes 409 active_session_exists via 7.4.4 and 409 stale_version `{details.current}` (D18) from 8.3.3/8.5.4/8.5.5 without an extra round trip; validation: none (no inputs) · TodayActiveSession.test.tsx + ActiveSessionCard.test.tsx, 10 cases: every kind×lifecycle Return-link mapping, 204 shows Start controls, Ready mounts the same card, staleNotice renders 'This session was updated in another tab' from the current (not stale) lifecycle; absorbs 8.10.4 from group 8b per D16; verify: `npm run test -w @attention-lab/web -- src/features/today/TodayActiveSession.test.tsx src/features/session/ActiveSessionCard.test.tsx` → 10 pass. after: 8.2.1, 8.2.2, 7.1.1, 7.4, 7.4.4
- [x] 8.3.1 Benchmark Ready — component: Ready, Checklist, ReplacementReasonField, ActiveSessionCard (also mounted here); state: `programs/current` (slots[].attempts per D22), `today`, `sessions/active`; useStartSession (7.4.4) owns the start mutation; api: `POST /sessions` (IK) — no intendedOutput/targetSeconds (D30: server forces 1200), 422 slot rules render the server message, active_session_exists → ActiveSessionCard; validation: replacementReason required 1–500 chars for a prior ineligible/excluded attempt, Start offered on or after the assigned date with a timing-deviation notice on late dates (D23), never before · Ready.test.tsx, 13 cases: date gate, slot.attempts-driven replacement/no-retake/two-attempt rules, benchmark body shape, active-session override, data-mode benchmark, Final A label; verify: `npm run test -w @attention-lab/web -- src/features/benchmark/Ready.test.tsx` → 13 pass. after: 7.1, 7.1.1, 7.4, 7.4.4, 4.2, 8.2.5, 2.7
- [x] 8.3.2 TimerDisplay — component: TimerDisplay, useEndChime, useRemaining, useMilestoneAnnouncements (7.1.6); state: hidden flag seeded from `preferences.hideTimerDefault`, chime-fired ref, milestone last-announced ref; api: `GET /me` (hideTimerDefault, endChime, milestoneAnnouncements — D22/D39); validation: none beyond the hide toggle · TimerDisplay.test.tsx, 8 cases: hidden renders no digits, chime fires once at zero, no aria-live on running digits, milestone announcement gated strictly by `preferences.milestoneAnnouncements`, reduced-motion strips transitions, remaining derives from server fields across a pause+advance; verify: `npm run test -w @attention-lab/web -- src/features/focus/TimerDisplay.test.tsx` → 8 pass. after: 7.1.1, 7.1.6, 7.2, 7.4
- [x] 8.4.1 Recall — component: Recall, RecallPoints; state: phase confirm/writing, startedAtMs from `serverNowMs` (7.2.1, D27 — never client `Date.now`), controlled five points, saveRecall mutation; api: `GET /sessions/{id}`, `POST /sessions/{id}/recall` (IK) — 200 replay, 409 different-content → notice+scoring, 422 `recall_before_interval_end`; validation: points ≤2000 chars, blank allowed, Save gated on confirm; incomplete attempts (`completeInterval` false) additionally offer 'Skip recall' → scoring with no POST at all (D25) · Recall.test.tsx, 11 cases: countdown gating, serverNowMs-based timing, 3:00 overrun stays editable, locked-redirect, 409 notice, Skip-recall-vs-Start-recall by completeInterval; verify: `npm run test -w @attention-lab/web -- src/features/benchmark/Recall.test.tsx` → 11 pass. after: 7.1.1, 8.3.2, 7.1, 7.2.1, 7.4, 2.7
- [x] 8.4.2 Scoring — component: Scoring, PointRow, BenchmarkReviewPage; state: per-point answers, derived complete flag, preview score, `recallMissing = !recallLockedAt && !completeInterval`; api: `GET /sessions/{id}` only; validation: every non-blank point answered before parent's Finalize enables; recallLockedAt null + complete interval → 'Recall must be saved first' + link, no controls; recallLockedAt null + incomplete interval → 'Recall not saved — this attempt will be recorded with recall missing', no controls, no link back (D25) · Scoring.test.tsx, 10 cases: both null-recall states distinguished, read-only locked points, blank-scored-0, self-reported label, delayed/overrun flags, recall-missing emits complete=true with no recallScores, tab order; verify: `npm run test -w @attention-lab/web -- src/features/benchmark/Scoring.test.tsx` → 10 pass. after: 8.4.1, 7.1.1, 7.4, 2.7
- [x] 8.4.3 Count fields & first-switch preview — component: CountFields, CountField, FirstSwitchPreview; state: {episodeCount, countMethod, externalCount, mindWanderingCount, estimateMinutes} strings, derived firstSwitch via packages/shared `firstSwitch.ts`; api: reads `GET /sessions/{id}` events/tallies only; validation: counts integer ≥0 or blank (never coerced), estimate 0–20 or blank · CountFields.test.tsx + toFinalizeReview.test.ts, 11 cases: no-events blank vs event-prefilled S, voided events excluded from prefill, paper-tally retrospective, explicit-0 → '20+, capped', retrospective-without-estimate → Unknown never 20+, estimate→seconds, event-timed first switch, blank-vs-explicit-zero E; verify: `npm run test -w @attention-lab/web -- src/features/benchmark/CountFields.test.tsx src/features/benchmark/toFinalizeReview.test.ts` → 11 pass. after: 8.4.2, 7.1.1, 7.4, 2.2, 2.7
- [x] 8.4.4 Disruption, conditions & eligibility preview — component: DisruptionField, ConditionsFields, EligibilityPreview, IncompleteBanner; state: {materiallyDisrupted, disruptionNote, conditions, conditionsConfirmed}, derived eligibility preview via packages/shared `eligibility.ts`; api: reads `GET /sessions/{id}` (observedConditions, completeInterval) and `GET /programs/current` (assignedLocalDate); no writes; validation: disruption required, note ≤500, conditions-confirm required for parent's canFinalize · ReviewAttestation.test.tsx, 9 cases: unanswered-then-capped-note, disruption-Yes copy, accommodation checkbox, defaults from observedConditions, canFinalize gating, incomplete banner with elapsed time, preview never bare 'Eligible', keyboard traversal; verify: `npm run test -w @attention-lab/web -- src/features/benchmark/ReviewAttestation.test.tsx` → 9 pass. after: 8.4.3, 7.1.1, 7.4, 2.3, 2.7
- [x] 8.4.5 Finalize & eligibility summary — component: FinalizeBar, EligibilitySummary; state: useFinalizeSession (7.3.5) owns the mutation, Idempotency-Key, flush/retry/mismatch state — no local mismatch or retry logic; local reviewNote (D31, optional, ≤500, sent only when non-blank); api: useFinalizeSession.finalize → POST /sessions/{id}/finalize (IK), 409 event_count_mismatch handled entirely inside 7.3.5; allowed on an incomplete attempt with recall missing, no recallScores sent (D25); validation: disruption answered + scoring complete + conditions confirmed, recall lock NOT required when incomplete · Finalize.test.tsx, 11 cases: disabled states, incomplete-without-recall enables and omits recallScores, fieldErrors, three-mismatch unsaved_entries, combined interval_incomplete/recall_missing/scoring_incomplete summary, blank-S-not-0, no % text, Continue routing; verify: npm run test -w @attention-lab/web -- src/features/benchmark/Finalize.test.tsx → 11 pass. after: 8.4.2, 8.4.3, 8.4.4, 7.1.1, 7.3.5, 7.4, 2.3, 2.7
- [x] 8.5.1 useSessionEvents & EventButtons — component: EventButtons, Tallies, AgentCheckConfirm; state: {events[], voided set} reducer, outbox subscription, conditional visibilitychange listener gated by preferences.visibilityContext; api: GET /sessions/{id} seed, POST /sessions/{id}/events via outbox, .../void; validation: one click = one event, undo only the latest non-voided event · EventButtons.test.tsx + tallies.test.ts, 11 cases: agent-check alsoOffTask tallying (D11), visibilityContext off → no listener, visibilityContext on → one 'visibility' event excluded from every tally (D39), undo unsent-vs-sent, benchmark variant has no Agent-check, paused-time excluded from elapsedMs, tallies() excludes voided/visibility with no total; verify: npm run test -w @attention-lab/web -- src/features/focus/EventButtons.test.tsx src/features/focus/tallies.test.ts → 11 pass. after: 7.1.1, 7.2, 7.3, 7.4, 2.7
- [x] 8.5.2 SyncStatus — component: SyncStatus, useOutboxStatus; state: outbox-derived 'saved'|'pending'|'local_write_failed', ack tracking by clientEventId, in-memory direct-send queue; api: `POST /sessions/{id}/events` acks (`accepted[]`/`duplicates[]`) observed via the outbox, 422 `impossible_offset` marks one entry rejected without looping; validation: Saved only after ack, never on dispatch, no ids/keys/realm text rendered · SyncStatus.test.tsx, 9 cases: Pending+Retry, dispatched-not-acked stays Pending, Saved after ack, could-not-save + keep-trying-directly, rejected-entry copy, no identifiers, aria-live polite with no per-tick re-render; absorbs 8.10.1 from group 8b per D16; verify: `npm run test -w @attention-lab/web -- src/features/focus/SyncStatus.test.tsx` → 9 pass. after: 7.1.1, 7.3, 5.3
- [x] 8.3.3 Benchmark Running — component: Running, EventButtons(benchmark), Tallies, TimerDisplay, SyncStatus; state: useSessionEvents reducer, remaining from lib/clock, transition mutation; api: `POST /sessions/{id}/events` via outbox, `.../events/{id}/void`, `POST /sessions/{id}/transitions {type:'end'}` both at the deadline's 'I'm ready for recall' and on Stop early (D24) — 409 stale → ActiveSessionCard's notice, no second transition; validation: no pause control, no Abandon control (shared 8.10.7 in SessionLayout), elapsedMs only from lib/clock · Running.test.tsx, 11 cases: no pause/abandon controls, undo, hidden-tab no-event, deadline copy with no premature navigation, end-transition-then-navigate ordering, Stop-early routing, stale-409 notice; verify: `npm run test -w @attention-lab/web -- src/features/benchmark/Running.test.tsx` → 11 pass. after: 8.3.1, 8.3.2, 8.5.1, 8.5.2, 8.2.5, 7.1.1, 7.2, 7.3, 7.4, 2.7
- [x] 8.3.4 Playwright hidden-tab/undo — impl: e2e/benchmark-running.spec.ts uses e2e/support/clock.ts's `advance`/`hideTab`/`showTab` (7.1.5, pairing `page.clock` with `POST /demo/clock` so server and browser stay aligned per D5/D8); 15-minute hidden interval creates zero events; double-Record-then-Undo leaves exactly one non-voided off_task event server-side; asserts no pause or abandon control on Running (8.10.7 owns Abandon); exports `startBaselineA(page, request)`; verify: `npm run build && npx playwright test e2e/benchmark-running.spec.ts` → '1 passed'. after: 8.3.1, 8.3.3, 8.1.4, 7.1.5, 3.5, 4.2, 5.1, 5.2, 5.3, 5.4, 1.5
- [x] 8.4.6 Playwright benchmark review — impl: e2e/benchmark-review.spec.ts uses e2e/support/clock.ts's `advance` (7.1.5); proves 'I'm ready for recall' posts `transitions{type:'end'}` (D24) before navigating; scoring inoperable before Save recall; Finalize refused server-side (400/422 fieldErrors.materiallyDisrupted) and client-side without a disruption answer; a full attempt finalizes Eligible with T '20+, capped', no %; a second, Stop-early attempt offers 'Skip recall' (D25), finalizes with interval_incomplete + recall_missing + scoring_incomplete and no recall score; verify: npm run build && npx playwright test e2e/benchmark-review.spec.ts → '1 passed'. after: 8.4.1, 8.4.2, 8.4.3, 8.4.4, 8.4.5, 8.3.4, 7.1.5, 5.7, 5.8, 4.2, 1.5
- [x] 8.5.3 Focus frame — component: Focus, SessionHeader; state: `['sessions', id]` query, remaining from `useRemaining`, transition mutation for the deadline Review action only; api: `GET /sessions/{id}` (404→/today), `POST /sessions/{id}/transitions {type:'end'}` on the deadline's Review click (D24) before navigating to `/review/:id`, 409 stale → ActiveSessionCard's notice; validation: none (no inputs) · Focus.test.tsx, 10 cases: no nav landmarks, no 'focused/attention score' text, mount-mid-block remaining, post-target awaiting-review copy with no completed wording, Review posts-then-navigates ordering, no self-finalize on zero, 404 redirect, leave-guard on URL nav, stale-409 notice, SyncStatus rendered; verify: `npm run test -w @attention-lab/web -- src/features/focus/Focus.test.tsx` → 10 pass. after: 7.1.1, 8.2.5, 8.3.2, 8.5.1, 8.5.2, 7.1, 7.2, 7.4, 2.7
- [x] 8.2.6 Playwright Today flows — impl: e2e/today-start-practice.spec.ts loads the 'working-day' scenario via e2e/support/demo.ts (7.1.5), starts practice in ≤3 actions, asserts `sessions/active` 200 with the typed output; second page on `/today` shows ActiveSessionCard (8.2.5) with Return, no Start; narrow 390×844 viewport after reset shows bottom nav, single-column blocks, no horizontal scroll; verify: `npm run build && npx playwright test e2e/today-start-practice.spec.ts` → '1 passed'. after: 8.2.1, 8.2.2, 8.2.3, 8.2.4, 8.2.5, 8.5.3, 7.1, 7.1.5, 3.5, 4.5, 5.1, 5.2, 5.4, 2.8, 1.5
- [x] 8.5.4 TransitionControls — component: TransitionControls, useTransition; state: transition mutation keyed by expectedVersion, optimistic pause-freeze rolled back on error; api: `POST /sessions/{id}/transitions` (pause/resume/end) — 409 stale → refetch and render ActiveSessionCard's notice (8.2.5/D18), 422 → message; validation: every transition carries the current version, confirm on finish-early; NO Abandon control here (shared 8.10.7 in SessionLayout) · TransitionControls.test.tsx, 9 cases: pause/resume freeze+resume math, finish-early running/paused, no-abandon-here assertion, stale-409 handoff to ActiveSessionCard, optimistic rollback, one-primary-control layout, 422 message; verify: `npm run test -w @attention-lab/web -- src/features/focus/TransitionControls.test.tsx` → 9 pass. after: 8.5.3, 8.2.5, 7.1.1, 7.3, 7.4, 2.7
- [x] 8.5.5 AgentPanel — component: AgentPanel, PlanFields; state: controlled plan draft, open/closed flag, putAgentPlan mutation + shared useTransition (8.5.4); api: `PUT /sessions/{id}/agent-plan` — 409 stale → server values + notice; `POST /sessions/{id}/transitions {type:'pause', reason:'planned break'}`; validation: each field ≤200 chars, all optional · AgentPanel.test.tsx, 8 cases: collapsed default, save with expectedVersion, stale-plan keeps first values, Return-to-my-task is nav/transition-free, screen-free break pauses via the shared hook, no credential/output/log inputs, checkpoint default, 201-char field blocked; verify: `npm run test -w @attention-lab/web -- src/features/focus/AgentPanel.test.tsx` → 8 pass. after: 8.5.4, 7.1.1, 7.4, 2.7
- [x] 8.5.6 Playwright screen-free break — impl: e2e/focus-session.spec.ts loads the 'working-day' scenario via e2e/support/demo.ts and advances time via e2e/support/clock.ts's advance (7.1.5); screen-free break pauses (lifecycle 'paused', remaining frozen), Resume shows pausedSeconds ≥120; Agent-check-also-off-task yields server tallies offTask 1 / agentChecks 1 with one stored agent_check event, no on-screen '2'; SyncStatus (8.5.2) reads 'Saved' only after acknowledgement, never while the batch request is held; verify: npm run build && npx playwright test e2e/focus-session.spec.ts → '1 passed'. after: 8.5.1, 8.5.2, 8.5.3, 8.5.4, 8.5.5, 8.2.2, 8.3.4, 7.1.5, 3.5, 5.1, 5.2, 5.3, 5.4, 5.6, 2.8, 1.5
- [x] 8.6.2 PracticeReview screen consuming useFinalizeSession (7.3.5) — component: PracticeReview, OutputQualityField, CountField, OutputNoteField, ReviewNoteField under SessionLayout; state: GET /sessions/{id} query, reducer form state, hook status drives Save; api: GET /sessions/{id} (404/finalized handled), POST finalize only via 7.3.5, `timing.elapsedSeconds` and `tallies` {offTask,external,agentChecks} taken verbatim (D20); validation: outputQuality required, counts blank≠0, outputNote/reviewNote each optional and independent (D31) · PracticeReview.test.tsx, 13 cases incl. 'prefills from server tallies', 'blank counts omit fields', 'recorded-time from timing.elapsedSeconds', 'focused never appears'; verify: `npm run test -w @attention-lab/web -- features/review/PracticeReview` → 13 passed after: 7.1.1, 7.1.4, 7.3, 7.3.5, 7.4, 5.2, 5.8, 2.7
- [x] 8.6.3 Playwright: Partly review journey, early finish, keyboard-only review — impl: e2e/practice-review.spec.ts using advance/reset from e2e/support/clock.ts (7.1.5, not a new demoClock.ts); Test 1 Partly with blank notes and null episodeCount; Test 2 early finish reading '8 min recorded of 15' from timing.elapsedSeconds; Test 3 keyboard walk through output quality → S → E → agent checks → output note → review note → Save with visible focus indicators; verify: `npx playwright test e2e/practice-review.spec.ts` → 3 passed after: 8.6.2, 7.1.5, 8.2, 8.5, 4.5, 3.5, 2.8, 1.5
- [x] 8.7.1 CheckinForm core: sleep, phone, desktop, save, status, stale conflict — component: CheckinForm, CheckinStatus, DeviceMinutesField under RailLayout; state: GET days/{date} (D22 all-null shape/version 0 when none), reducer form state, PUT with expectedVersion; api: headline inputs post platform:'all' rows (D36), disabled+summed when detail rows exist, 422 feed_platform_conflict → headline field, 409 stale → Re-apply my values; validation: minutes integer≥0 or blank, blank≠0, headline/detail mutually exclusive · CheckinForm.test.tsx, 14 cases incl. 'platform all row', 'detail row disables headline and sums', 'feed_platform_conflict on headline', 'D22 no-row shape, expectedVersion 0'; verify: `npm run test -w @attention-lab/web -- features/checkin/CheckinForm` → 14 passed after: 7.1.1, 7.1.3, 7.4, 6.1, 4.2, 2.7, 2.6
- [x] 8.7.2 FeedRows detail, optional fields and feed totals preview — component: FeedRows, FeedRow, FeedTotals, OptionalFields inside the 8.7.1 Collapsible; state: rows in the shared reducer, first/last detail row toggles the device headline enabled state (D36); api: PUT feed rows exclude platform 'all' from detail rows, 422 subset → short-video field, 422 feed_platform_conflict → row group; validation: shortVideo≤minutes, platform required and 'all' rejected on detail rows · FeedRows.test.tsx, 11 cases incl. 'all rejected on detail row', 'detail row disables headline', 'device-minutes totals via feed.ts only'; verify: `npm run test -w @attention-lab/web -- features/checkin/FeedRows` → 11 passed after: 8.7.1, 2.6, 6.1
- [x] 8.7.3 Playwright: check-in journey, subset rejection, skipped day, edit twice, phone viewport — impl: e2e/checkin.spec.ts using reset() from e2e/support/clock.ts (7.1.5); Test 1 phone 55 saved as a platform:'all' row with neutral copy; Test 2 desktop detail row (no headline conflict) rejects short-video>minutes then saves and disables the desktop headline; Test 3 sleep-only missing feed; Test 4 skipped day null not 0; Test 5 edit-twice version increments; Test 6 phone-viewport single column, 44px targets; verify: `npx playwright test e2e/checkin.spec.ts` → 6 passed after: 8.7.2, 7.1.5, 8.2, 4.5, 3.5, 6.2, 2.8, 1.5
- [x] 8.8.1 Progress shell, samples line and AttemptTable — component: Progress, SamplesLine, AttemptTable, ProgressEmptyState under RailLayout; state: programs/current + report queries, no client-side derived scores (D4); api: GET /programs/current (404→empty state, no zero cards), GET /programs/{id}/report, 422 realm mixing → error panel; unfinalized rows show their lifecycle word instead of blank-as-null scored columns, each finalized row mounts AmendmentDialog (8.8.6); validation: read-only · Progress.test.tsx, 10 cases incl. 'Not reported never 0', 'T none_capped vs known vs Unknown', 'unfinalized row shows lifecycle, no Explain or exclude'; verify: `npm run test -w @attention-lab/web -- features/progress/Progress.test` → 10 passed after: 7.1.1, 7.1.3, 7.4, 6.2, 4.2, 2.7
- [x] 8.8.2 ResultState card, comparison figures and comparability warnings — component: ResultState, ComparisonFigures, ComparabilityWarnings, pure presentation from report props (D4); state: none client-derived; api: consumes report.resultState/comparison/warnings; validation: read-only · ResultState.test.tsx, 12 cases incl. 'zero baseline → not applicable, no Infinity/NaN/100%', 'improvement headline Fewer reported switches (D38)'; verify: `npm run test -w @attention-lab/web -- features/progress/ResultState` → 12 passed after: 8.8.1, 2.4, 2.8, 6.2
- [x] 8.8.3 Practice and daily trend sections with Recharts and exact values — component: PracticeTrend, DailyTrend, ExactValuesTable; state: pure presentation from report.practice/report.days; api: no additional requests, feed labels via feed.ts (2.6); validation: read-only · Trends.test.tsx, 9 cases incl. 'Not reported never 0, chart datum null', 'source labels Estimate/From device report', 'aria-hidden charts with table equivalent', 'no file outside features/progress imports recharts'; verify: `npm run test -w @attention-lab/web -- features/progress/Trends` → 9 passed after: 8.8.1, 6.2, 2.6
- [x] 8.8.4 ExportPreview with CSV/Markdown preview and download — component: ExportPreview, FormatToggle; state: query keyed by format, staleTime/gcTime 0 (no-store honored client-side); api: GET /programs/{id}/export?format=csv|markdown; validation: read-only · ExportPreview.test.tsx, 6 cases incl. 'demonstration label first line', 'blank count row has empty S cell', 'gcTime 0 retains nothing after unmount'; verify: `npm run test -w @attention-lab/web -- features/progress/ExportPreview` → 6 passed after: 8.8.1, 6.3
- [x] 8.8.5 Playwright: Progress scenario sweep, no-score guard, export download — impl: e2e/progress.spec.ts iterating every fixture scenario with its exported expected.resultState (D35); asserts RESULT_STATE_COPY, samples line, self-reported label, no invented-score wording per scenario; comparable-change and zero-baseline math assertions; CSV/Markdown download checks; verify: `npx playwright test e2e/progress.spec.ts` → 8 scenario cases + 2 export cases passed after: 8.8.2, 8.8.3, 8.8.4, 3.5, 2.8, 6.3, 1.5
- [x] 8.8.6 AmendmentDialog: explain or exclude a finalized attempt — component: AmendmentDialog, AmendmentList mounted from finalized AttemptTable rows (8.8.1); state: dialog draft {reason, excludeFromReport}, mutation invalidates the report query; api: POST /sessions/{id}/amendments (5.9.1) 201, 400→reason field, 409 not_finalized → closes with message; validation: reason required, checkbox boolean, no client-side eligible/exclusionReasons recompute (D4) · AmendmentDialog.test.tsx, 8 cases incl. 'empty reason blocks submit', 'excluded_by_amendment appears only after report refetch', 'not rendered on unfinalized rows'; verify: `npm run test -w @attention-lab/web -- features/progress/AmendmentDialog` → 8 passed after: 8.8.1, 5.9.1
- [x] 8.9.1 ResearchCards screen with demonstration labels and deliberate links — component: ResearchCards, ResearchCard under RailLayout; state: GET /research/cards, staleTime Infinity, no retry/refetch; api: GET /research/cards, error → Cards unavailable; validation: only http(s) URLs become links · ResearchCards.test.tsx, 7 cases; verify: `npm run test -w @attention-lab/web -- features/research/ResearchCards` → 7 passed after: 7.1.1, 7.1.3, 7.4, 6.4, 2.8
- [x] 8.9.2 Settings Preferences: timezone, timer defaults, milestone announcements, visibility opt-in — component: Settings, Preferences, PreferenceSwitch, TimezoneSelect, EditMaterialsLink under RailLayout; state: GET /me draft (5 fields), dirty flag; api: PATCH /me/preferences sends only changed fields incl. milestoneAnnouncements (D39), GET /programs/current gates the Edit benchmark materials → /setup/readiness link (D38); validation: IANA timezone, booleans, nothing sent until Save · Preferences.test.tsx, 8 cases; verify: `npm run test -w @attention-lab/web -- features/settings/Preferences` → 8 passed after: 7.1.1, 7.1.3, 7.4, 3.5
- [x] 8.9.3 DemoControls: demo clock, scenario loader, reset — component: DemoControls, DemoClockPanel, ScenarioLoader, ResetPanel, local-demo only; state: offset draft, mutations clear the query cache; api: POST /demo/clock, POST /demo/scenarios/{name}/load with no body (D22, the AlertDialog is the confirmation), POST /demo/reset then outbox purge (7.3) and every finalize:{sessionId} key cleared (7.3.5); validation: integer offset, dialog-gated load/reset · DemoControls.test.tsx, 8 cases incl. 'Skip to Day 14 targets final slot A planned time else 00:00', 'reset clears finalize keys'; verify: `npm run test -w @attention-lab/web -- features/settings/DemoControls` → 8 passed after: 8.9.2, 3.5, 2.8, 2.1, 4.2, 7.3, 7.3.5
- [x] 8.9.4 Playwright: navigation, research hidden in session, new-tab source, demo controls, banner — impl: e2e/research-settings.spec.ts; 8 tests covering rail navigation outside session mode, Research hidden during practice and benchmark (route-leave guard), Read source in a new tab, demo reset/load/skip-to-Day-14 via DemoControls (8.9.3, posts with no body per D22), and demo banner visible without scrolling on /progress, /research, /settings, /checkin and /review; verify: `npx playwright test e2e/research-settings.spec.ts` → 8 passed after: 8.9.1, 8.9.3, 8.6.3, 8.2, 8.3, 8.5, 7.1.3, 7.1.4, 7.4, 3.5, 2.8, 1.5
- [x] 8.9.5 Change practice duration: manual revision with reason — component: ChangePracticeDuration, DurationField mounted in Settings; state: GET /programs/current governing settings as default, local draft, dirty flag; api: POST /programs/{id}/revisions {effectiveDay: today, settings: {...current, practiceTargetSeconds}, reason} (4.4.1) → invalidate programs/current and today; 400 fieldErrors.reason, 422 effective_day_in_past, 409 program_terminal hides control; validation: duration ∈ {300,600,900,1200,1500}, reason required · ChangePracticeDuration.test.tsx, 8 cases; verify: `npm run test -w @attention-lab/web -- features/settings/ChangePracticeDuration` → 8 passed after: 8.9.2, 4.4.1
- [x] 8.10.2 ClockGapPrompt with continued, uncertain and save-incomplete resolutions — component: ClockGapPrompt, useClockGap wrapping the 7.2 detector, mounted from SessionLayout; state: dialog open flag, the clock_gap event's details.resolution drives reload re-prompt (D26), clock re-seed from server remaining after resolution; api: POST /sessions/{id}/clock-gap + clock_gap event via outbox; validation: gapSeconds>60, one resolution, no dismissal · ClockGapPrompt.test.tsx, 8 cases incl. 'unresolved event re-prompts on reload, resolved does not', 'save_incomplete navigates to incomplete review, recall skippable (D25)'; verify: `npm run test -w @attention-lab/web -- features/focus/ClockGapPrompt` → 8 passed after: 7.1.1, 8.6.2, 7.1.4, 7.2, 7.3, 7.4, 5.5, 5.3, 8.4
- [x] 8.10.7 AbandonSession control: confirm, abandon transition, buffer and draft purge — component: AbandonSession, useAbandonSession wrapping abandonWithPurge (7.3.4), mounted in SessionLayout for every session-mode screen; state: dialog flag, transition+purge owned by 7.3.4, finalize key (owned by 7.3.5) cleared here; api: POST /sessions/{id}/transitions type abandon via 7.3.4, 409 stale → no second transition; validation: confirmation required, reason optional, copy per D38 · AbandonSession.test.tsx, 6 cases incl. 'never renders completed text', 'purges outbox and finalize key'; verify: `npm run test -w @attention-lab/web -- features/focus/AbandonSession` → 6 passed after: 7.1.1, 7.3.4, 7.1.4, 7.3, 7.4, 5.4
- [x] 8.10.8 Playwright: offline pending/retry, simulated sleep, reload, two contexts, abandon — impl: e2e/recovery.spec.ts using advance/reset from e2e/support/clock.ts (7.1.5); 10 tests covering SyncStatus pending/retry (8.5.2), offline start (useStartSession, 7.4.4), clock-gap Yes/uncertain variants (8.10.2), reload restore via SessionModeProvider (7.4.2), second-tab/two-tabs-end via ActiveSessionCard (8.2.5), and abandon purge (8.10.7); verify: `npx playwright test e2e/recovery.spec.ts` → 10 passed after: 8.5.2, 8.10.2, 7.4.2, 8.2.5, 7.3.5, 7.4.4, 8.10.7, 8.6.3, 7.1.5, 8.3, 8.4, 8.5, 3.5, 2.8, 1.5
## 9. End-to-end acceptance and accessibility

- [x] 9.1.1 Acceptance harness: Playwright `acceptance` project (workers 1, fullyParallel false, retries 0, baseURL `127.0.0.1:8787`) — impl: `webServer` runs `npm run build -w @attention-lab/web` then the API's `tsx` entry (3.2.5) with `WEB_DIST_DIR=apps/web/dist`, `IDENTITY_MODE=local-demo`, `DATABASE_URL` on the `db:up` Postgres (D37), isolated per test via `POST /demo/reset`; support helpers (`demo`/`clock`/`copy`/`a11y`) live in 7.1.5 and are only imported; `harness.spec.ts` covers reset, every `DEMO_SCENARIO_NAMES` slug, `readyProgram()`, `completePracticeBlock()`, skip-to-Day-14; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/harness.spec.ts` → 4 passed. after: 1.2, 1.3, 1.4, 1.5, 2.7, 2.8, 3.2.5, 3.5, 4.1, 4.2, 4.3, 5.1, 5.2, 5.4, 5.8, 6.2, 7.1.5, 8.2, 8.3
- [x] 9.1.2 Acceptance helper verification: clock, hidden tab, copy and axe negative controls — impl: `e2e/support/helpers.spec.ts` imports installClock/advance/sleep/hideTab/showTab (clock.ts), expectNoPunitiveCopy/expectDemoBanner/expectNoTechnicalIds (copy.ts) and expectNoSeriousViolations/expectFocusVisible (a11y.ts), all created by 7.1.5 (D16) and never redefined; 6 cases: 60 s advance matches `timing.remainingSeconds` (D20) within 1 s, sleep(300) raises the clock-gap prompt and a server clock_gap event while sleep(30) raises nothing (D5 60 s threshold), hideTab/showTab toggle visibilitychange, copy and axe assertions pass on Today and fail on injected negative controls (word 'streak', a UUID, an unlabeled icon button); verify: `npx playwright test -c e2e/playwright.config.ts e2e/support/helpers.spec.ts` → 6 passed. after: 9.1.1, 7.1.5, 7.2, 5.2, 5.5, 8.2, 8.5, 8.10
- [x] 9.1.3 Journey: new user from reset through setup and readiness to Progress — impl: `e2e/acceptance/new-user.spec.ts`, 6 cases from auto reset: baseline-pending copy with no percent/0/attention wording, Save refused until timezone confirmed and a blank feed estimate stored null, 5-minute duration keeps the 600 s Days 1-3 ceiling, readiness complete reaches Today's baseline-A next action, Progress shows 0-of-2 baseline samples, demo banner visible without scrolling on all four screens; field-level validation is 8.1's job, not repeated; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/new-user.spec.ts` → 6 passed. after: 9.1.1, 9.1.2, 8.1, 8.2, 8.8
- [x] 9.1.4 Journey: Day 0 baseline A and B with events, recall, scoring, Progress — impl: `e2e/acceptance/baseline-day.spec.ts` via `readyProgram()`; adds reusable `e2e/support/benchmark.ts` `runBenchmark()` driving ready→Start→events→deadline→recall→scoring→Finalize; 6 cases: ready-screen copy and 204 until Start, off-task/external/Undo tallying (voided row per D9), recall lock and Accurate scoring with blank points, S/E prefill and first-switch states (known 462 s vs Unknown, never 20+), Progress shows 2-of-2 eligible baselines with self-reported labels and CSV rows, a Day-0 practice block stays out of the baseline samples; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/baseline-day.spec.ts` → 6 passed. after: 9.1.1, 9.1.2, 8.2, 8.3, 8.4, 8.8, 4.2, 5.1, 5.3, 5.7, 5.8, 6.2, 6.3
- [x] 9.1.5 Journey: working day on Day 4 with agent waiting, pause, episode, review — impl: `e2e/acceptance/working-day.spec.ts`, `demo.load('working-day')` (Day 4, one 15-min block left); 7 cases: Today's next action and 15-min target, empty-output Start blocked, Start only after server ack (`waitForResponse`), Focus is session-mode (no nav/research), agent plan save plus screen-free-break pause/resume with paused time excluded from the 900 s target, one departure = one episode with agent-check-also-off-task tallying 2 off-task/1 agent-check never 3 (D11), review prefills and finalize with neutral copy; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/working-day.spec.ts` → 7 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6, 5.2, 5.4, 5.6, 5.8
- [x] 9.1.6 Journey: comparable change and mixed result on Progress with export — impl: `e2e/acceptance/comparison-outcomes.spec.ts`; `demo.load('comparable-change')` shows 5→3 switches, 40% reduction, recall maintained, 'fewer reported switches' headline, cause note, no forbidden framing (`attention +`, confidence interval, p-value); `demo.load('mixed-result')` shows same switch numbers with recall 4→2 and 'these results are mixed' copy, no success banner; export preview and CSV/Markdown downloads open with the demo label, carry per-attempt fields and `no-store`; a Day 14 practice block leaves the comparison unchanged; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/comparison-outcomes.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 8.8, 6.2, 6.3, 5.1, 5.4, 5.8
- [x] 9.1.7 Journey: missing final and zero baseline guards on Progress — impl: `e2e/acceptance/comparison-guards.spec.ts`; `demo.load('missing-final')` shows the insufficient-data copy with no percent sign, 1-of-2 final samples, the ineligible attempt's reason from `EXCLUSION_REASON_COPY`, and CSV cells render null values empty, never 0; `demo.load('zero-baseline')` shows 'a percentage reduction does not apply', absolute 0→0 with no Infinity/NaN/100%/-%/undefined, and every attempt's firstSwitch is `none_capped` ('20+, capped') with no mean-T display; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/comparison-guards.spec.ts` → 4 passed. after: 9.1.1, 9.1.2, 8.8, 6.2, 6.3
- [x] 9.1.8 Journey: recovery with unsynced batch, pending, retry, saved, early finish — impl: `e2e/acceptance/recovery.spec.ts`; `demo.load('recovery')` (an existing running practice session); 7 cases: Focus resumes the scenario session, offline records show Pending/Retry with tally updates <100 ms and the timer still counting, reconnect+Retry reaches Saved with exactly two server events and an empty outbox, re-sending the same batch stores nothing twice, sync state is plain words with no technical id, IndexedDB failure shows 'could not be saved on this device' yet the event still reaches the server, Finish early yields `completeInterval` false with neutral copy and the next block still available; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/recovery.spec.ts` → 7 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6, 8.10, 7.3, 5.3, 5.4, 5.8
- [x] 9.1.9 Journey: timing deviation — clock-gap reopened on load, its three resolutions, and date deviation — impl: `e2e/acceptance/timing-deviation.spec.ts`; `demo.load('timing-deviation')` loads a running baseline A with an unresolved 300 s clock_gap already recorded (D35); opening it re-opens the prompt titled "Did the interval continue uninterrupted?" with 4 options (D26); "Not sure"→uncertain/ineligible timer_uncertain; "Yes"→ok with the gap recorded and a later 25-min recall delay flagged not excluded; "Save as incomplete"→incomplete review where recall may be skipped (D25), yielding interval_incomplete+timer_uncertain(+recall_missing+scoring_incomplete when skipped); Day-15 final start still offered with a timing-deviation notice (D23), finalizing exclusionReasons [timing_deviation]; verify: `npx playwright test -c e2e/playwright.config.ts e2e/acceptance/timing-deviation.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 9.1.4, 8.3, 8.4, 8.8, 8.10, 5.5, 5.7, 5.8, 6.3
- [x] 9.2.1 Invariant: hidden tab never creates an episode (server truth) — impl: `e2e/invariants/hidden-tab.spec.ts` using installClock/hideTab/showTab/advance (7.1.5); 4 cases: a 15-min hidden benchmark yields zero server events and remaining 05:00±1s on show, practice hidden/shown three times creates no event, visibility opt-in (`PATCH /me/preferences {visibilityContext:true}`) records a `visibility` event while all tallies and the review S field stay 0/blank, returning to the tab triggers zero `GET /sessions/active` requests (D15); verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/hidden-tab.spec.ts` → 4 passed. after: 9.1.1, 9.1.2, 8.3, 8.5, 8.6, 8.9, 7.4, 5.2, 5.3
- [x] 9.2.2 Invariant: reload and URL navigation recover the session without side effects — impl: `e2e/invariants/refresh.spec.ts`; 5 cases: practice reload matches `timing.remainingSeconds` (D20) within 1 s, a benchmark's saved event plus a buffered/aborted event both survive reload with the buffered one replayed exactly once and the outbox emptied, reload after the deadline stays awaiting_review with no review or completion wording (D5), two consecutive reloads never duplicate the replayed event, navigating away by URL during a running practice shows the leave guard "A session is in progress" / "Return to session" / "Leave anyway" (D38) and Return to session lands back on /focus/:id with the session unchanged; verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/refresh.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 8.3, 8.5, 8.10, 7.2, 7.3, 7.4, 5.2, 5.3
- [x] 9.2.3 Invariant: expiry, early stop and abandon never become completion — impl: `e2e/invariants/never-completed.spec.ts` via advance/sleep/runBenchmark; 6 cases: a benchmark slept past the deadline wakes to the gap prompt with no review or finalizedAt; "Not sure" persists timer_uncertain into the eligibility preview (D4) and finalize; "Yes" records ~300 s and proceeds normally; a practice slept across target stays not-completed on a second tab until saved; Stop early at 14:00 records interval_incomplete at 840 s via the D25 incomplete review (recall optional), no new session; Abandon leaves lifecycle abandoned, outbox emptied, neutral copy; verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/never-completed.spec.ts` → 6 passed. after: 9.1.1, 9.1.2, 9.1.4, 8.2, 8.3, 8.4, 8.5, 8.6, 8.10, 7.3, 5.4, 5.5, 5.8
- [x] 9.2.4 Invariant: idempotent start and finalize under lost responses and mismatches — impl: `e2e/invariants/idempotency.spec.ts`; 6 cases: a finalize count mismatch returns 409 `event_count_mismatch` (D19, details {expected,stored} per D18), and a same-key retry after supplying the missing event succeeds once; a browser 409 on finalize triggers an outbox flush and retry with an identical Idempotency-Key, reaching one finalized session; replaying the same key returns the unchanged result; a late post-finalize event carries a reconciliation warning without altering the review; offline or lost-response starts never start a timer; retrying yields exactly one session (200 replay or 409 `active_session_exists`, D18/D19); verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/idempotency.spec.ts` → 6 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6, 8.10, 3.4, 5.1, 5.3, 5.4, 5.8
- [x] 9.2.5 Invariant: Day 8 revision via Accept leaves earlier sessions unchanged — impl: `e2e/invariants/day8-revision.spec.ts`; `demo.load('working-day')`; a beforeEach builds qualifying Days 5-6 via `completePracticeBlock` x4, `setDay(8)`, snapshotting practice/comparison/resultState and current revision; 5 cases: Accept creates revision 2 effective Day 8 ('progression accepted') with a 20-min target, Days 4-6 sessions keep revision 1's `revisionId`/`targetSeconds`, a Day 8 session references revision 2 at 1200 s, Progress lists both revisions with the durations-vary note and unchanged comparison, and days without a check-in read 'not reported' with no 0 min; revision-without-reason is 4.4's job; verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/day8-revision.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6, 8.8, 2.5, 4.4, 4.5, 5.1, 5.4, 5.8, 6.2
- [x] 9.2.6 Invariant: progression suggestion timing, hold, Partly and missed days — impl: `e2e/invariants/progression.spec.ts`; `demo.load('working-day')`; a `qualify(days, overrides?)` helper composes setDay+completePracticeBlock; 5 cases: Days 5-6 qualify but Day 7's 15-min ceiling withholds the suggestion, a Day 7 with no sessions still lets Day 8 offer +5 min with neutral copy (missed days hold the suggestion, 2.5), Hold starts the next session at 900 s with no new revision and re-offers the suggestion afterward, a Partly output on Day 6 block 2 breaks qualification, two episodes in one Day 6 block break qualification; verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/progression.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6, 2.5, 4.4, 4.5, 5.1, 5.4, 5.8
- [x] 9.2.7 Invariant: second tab and stale writes conflict instead of overwriting — impl: `e2e/invariants/two-tabs.spec.ts`; `demo.load('working-day')`, two same-context tabs; 5 cases: a second tab shows the existing running session with no start control, a stale-version transition returns 409 `stale_version` (D19) with `details.current` (D18) and the session version advances exactly once, a stale agent-plan write and a stale check-in write are both rejected 409 `stale_version` with the winning value kept, and starting while awaiting review returns 409 `active_session_exists` naming the session via `details.activeSessionId`; verify: `npx playwright test -c e2e/playwright.config.ts e2e/invariants/two-tabs.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6, 8.7, 8.10, 4.5, 5.1, 5.4, 5.6, 6.1
- [x] 9.3.1 Axe and responsive checks on Setup, Today, Check-in, Progress, Research, Settings — impl: `e2e/a11y/axe-shell.spec.ts`; 11 cases: zero serious/critical axe violations on Setup, Readiness, Today, Check-in collapsed/expanded, Progress (comparable-change and missing-final), Research, Settings; Tab reaches Today/Progress/Research/Settings in relative order with focus visible; at 390×844 Check-in is single column with no horizontal scroll, every control ≥24×24 CSS px (WCAG 2.2 AA 2.5.8, D40) and the bottom nav in view; axe JSON attached on failure; verify: `npx playwright test -c e2e/playwright.config.ts e2e/a11y/axe-shell.spec.ts` → 11 passed with zero serious/critical violations. after: 9.1.1, 9.1.2, 7.1.5, 8.1, 8.2, 8.7, 8.8, 8.9
- [x] 9.3.2 Axe checks on benchmark, Focus and Review session screens — impl: `e2e/a11y/axe-session.spec.ts` using expectNoSeriousViolations/expectDemoBanner/installClock/sleep (7.1.5); 10 cases: zero serious/critical violations on Benchmark ready, Benchmark running (timer visible/hidden), Recall, Scoring/Review, Focus (panel closed/open, clock-gap prompt via sleep(300)), Practice review, plus the demo banner in viewport on every session screen; axe JSON attached; verify: `npx playwright test -c e2e/playwright.config.ts e2e/a11y/axe-session.spec.ts` → 10 passed with zero serious/critical violations. after: 9.1.1, 9.1.2, 7.1.5, 8.3, 8.4, 8.5, 8.6, 8.10
- [x] 9.3.3 Keyboard-only practice review with focus visible at every step — impl: `e2e/a11y/keyboard-review.spec.ts`; `demo.load('working-day')`, start block 2 entirely by keyboard from Today; 4 cases: Finish early reached within 20 Tabs, the post-target review's tab order (output → Yes/Partly/No → episodes → external → agent checks → note → Save) shows a visible focus indicator at every stop and ArrowRight selects Partly in the radio group, Enter on Save finalizes with Partly and Today shows completed-with-partial-output neutral copy, and no keyboard trap exists (Tab/Shift+Tab both leave the form at its ends); verify: `npx playwright test -c e2e/playwright.config.ts e2e/a11y/keyboard-review.spec.ts` → 4 passed. after: 9.1.1, 9.1.2, 8.2, 8.5, 8.6
- [x] 9.4.1 Live-region assertion: countdown emits no per-second announcements — impl: `e2e/a11y/timer-live-region.spec.ts`; a MutationObserver on aria-live/status/alert/log/timer regions logs to `window.__liveLog`; milestone announcements follow `preferences.milestoneAnnouncements` (D39, default false, 5:00/0:00 only); 5 cases: zero live-region mutations over 120 s on Focus/Benchmark countdowns (default pref) and over 60 s on Recall, a hidden timer keeps the remaining time out of the DOM and still reaches awaiting review, and with the preference enabled crossing 5:00 and 0:00 each produce exactly one polite mutation and no others; every test also proves the observer is live via the countdown text changing; verify: `npx playwright test -c e2e/playwright.config.ts e2e/a11y/timer-live-region.spec.ts` → 5 passed. after: 9.1.1, 9.1.2, 8.3, 8.4, 8.5
- [x] 9.4.2 Reduced motion disables transitions and animations — impl: `e2e/a11y/reduced-motion.spec.ts` with `page.emulateMedia({reducedMotion:'reduce'})`; an `expectNoMotion(page)` helper asserts zero transition/animation durations, `document.getAnimations().length===0` and no SVG animate elements; 4 cases: Today→Progress (`demo.load('comparable-change')`) runs no animations, Focus (`demo.load('working-day')`) agent-panel and clock-gap-prompt toggles run no animations, Progress charts (Recharts) render with animation disabled, and Check-in's expand control swaps content instantly; verify: `npx playwright test -c e2e/playwright.config.ts e2e/a11y/reduced-motion.spec.ts` → 4 passed. after: 9.1.1, 9.1.2, 7.1, 8.2, 8.5, 8.7, 8.8, 8.10
## 10. Documents and handoff

- [ ] 10.4.1 Finish LIMITATIONS.md with tested scope, thresholds and coverage table — impl: fill the ten headings with only what test runs printed, folding in every D40 item verbatim (receipts expire on the demo clock, graceful shutdown POSIX-only, recovery/hidden-tab verified under paired simulated clocks, 24×24 touch targets, research provenance abstract-reviewed pending confirmation), the AJV coerceTypes:false/removeAdditional:false/useDefaults:false decision (D22), and 'no write limiter in this change, 429 write_limit reserved' (D19); add scripts/check-limitations.mjs asserting the ten headings, ten coverage rows, and every required literal; verify: node scripts/check-limitations.mjs prints LIMITATIONS_OK; after: 1.1.2, 1.3.1, 2.9, 3.6, 4.5, 5.9, 6.4, 7.4, 8.10, 9.4.
- [ ] 10.3.1 Append the implementation decision-log entry to the PRD — impl: append one dated paragraph to '## 11. Decision log' (implementation authorized via HANDOFF.md's brief, built as openspec change build-initial-mvp; D2/D4/D5/D6 outcomes; D7's six defect resolutions in one sentence each — nullable counts, materially_disrupted attestation, separate recall write with 600s/210s flags, result-state precedence with 'unchanged' and the Mixed fixture as 6/4→3/3 with recall 4/4→2/2, observed_conditions on the attempt, identity mode as the auth gate; every result state has a fixture; no real measurement collected); nothing above it changes; verify: diff against the root commit shows zero deletions and exactly one new line matching '^+2026-09-\d\d: '; after: 10.4.1.
- [ ] 10.3.2 Append the implementation entry to the Technical PoC discussion record — impl: append one dated paragraph to '## 16. Discussion record' (implementation authorized; D1 npm-workspaces supersession; D3 pin verification recorded in LIMITATIONS.md; API-shape deltas vs the PoC outline — separate recall write D10, mutation_receipts idempotency D6, append-only events with a void marker D9, agent_check subtype D11, server-side demo clock D8, nullable review columns D7.1, observed_conditions D7.5; API runs from source under tsx serving the Vite build on one origin; Better Auth deferred to add-real-identity); §14's table untouched; verify: root-commit diff shows zero deletions and exactly one new '^+2026-09-\d\d: ' line; after: 10.4.1.
- [ ] 10.3.3 Append the implementation row to the protocol decision record — impl: insert one dated row into the '## Discussion and decision record' table immediately after the existing 'Pending' row and before the check-in paragraph (prototype built per build-initial-mvp; S/agent-check/first-switch/blank-count definitions implemented as stated; flexible cross-device feed policy retained unchanged; recall thresholds 600s/210s recorded as flags, chosen not validated; eligibility uses self-attestation; no data collected, no effect claimed, LIMITATIONS.md referenced); every earlier row untouched; verify: root-commit diff shows zero deletions, one LIMITATIONS.md mention, and a node position check confirms the row sits directly between 'Pending' and the check-in paragraph; after: 10.4.1.
- [ ] 10.5.1 Add the fresh-database full verification script — impl: scripts/verify-all.mjs runs, in order, a fresh attention-lab-pg container, db:up, a pg_isready wait, db:push, typecheck, test, build, e2e, each spawned with inherited stdio and timed, printed as a PASS/FAIL table, stopping at the first failure; root script verify:all runs it; verify: npm run verify:all exits 0 with PASS on all six stages from a freshly created container and a second consecutive run is also green (test-database bootstrap is idempotent); after: 1.4.1, 1.5.2, 1.5.3, 9.1, 9.2, 9.3, 9.4.
- [ ] 10.1.1 Rewrite README local setup and fix stale pointers — impl: replace the 'no application to run' text with the P0 prototype monorepo description (local-demo, synthetic data only), fix reading-order item 5 to HANDOFF.md, add '## Local setup' (Node >=22.9, npm 11, Docker; cp .env.example .env at repo root; install, db:up, db:push, dev:api+dev:web, test, e2e, verify:all; loopback-only, permanent demo banner, real mode refuses to boot, LIMITATIONS.md pointer), amend '## Integrity' to state MANIFEST.sha256 now reports README.md and CODEX-HANDOFF.md as failed (7 of 9 OK) and is deliberately not regenerated; verify: a clean clone installs/builds/typechecks cleanly, sha256sum -c prints 7 OK lines, the fixed reading-order link and the single Integrity-note mention of CODEX-HANDOFF.md are present; after: 1.1.2, 1.4.1, 1.5.3, 10.4.1, 10.5.1.
- [ ] 10.2.1 Correct CLAUDE.md to describe the implemented repository — impl: replace 'What this repository is' with the working P0 monorepo description (npm workspaces, D1); replace '## Commands' with the real root scripts — db:up, db:down, db:push, db:test, typecheck, typecheck:e2e, test, test:domain, test:api, build, dev:api, dev:web, e2e, e2e:install, verify:all — noting the API runs from source under tsx and sha256sum -c now passes 7 of 9; rewrite the no-code paragraph as history (lifted 2026-09-06); retitle the six defects 'settled (D7)' with resolutions; add scripts/check-claude-md.mjs verifying every named npm run command exists in root scripts and that Authority/Invariants/Style sections plus the Codex footer stay byte-identical to the root commit; verify: typecheck && test exit 0, then the script prints CLAUDE_MD_OK; after: 1.1.2, 1.5.2, 1.5.3, 10.4.1, 10.5.1.
- [ ] 10.5.2 Final green run and the what-works / what-was-tested / limitations summary — impl: after every other group 1–10 unit is done, run npm run verify:all once more from a fresh container plus check-limitations.mjs, check-pins.mjs, check-claude-md.mjs; deliver the closing message under exactly the headings 'What works' / 'What was tested' / 'Remaining limitations' copied from LIMITATIONS.md, the D3 pin-table step-back summary, and 'sha256sum -c passes 7 of 9', with no attention-score, percentage-improvement or significance language; verify: verify:all exits 0 with PASS on all six stages, all three check scripts print their OK tokens, and the response text matches none of /attention \+|significan|confidence interval|attention score/i; after: 10.1.1, 10.2.1, 10.3.1, 10.3.2, 10.3.3, 10.4.1, 10.5.1.
